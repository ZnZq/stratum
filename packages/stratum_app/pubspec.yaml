name: stratum_app
description: STRATUM — мобільний UI-шар над stratum_core (iOS та Android).
publish_to: none
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  stratum_core:
    path: ../stratum_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
