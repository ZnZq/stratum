# STRATUM — мобільна incremental/idle гра

Це передача проєкту з дизайн-сесії в claude.ai (серпень 2026). Усі рішення нижче — узгоджені з
власником проєкту (Олексій) і НЕ підлягають перегляду без його явного запиту. Твоє завдання —
реалізація, а не редизайн механік.

## Стек (вирішено)
- **Гра: Flutter/Dart.** Таргети — iOS та Android. Веб-версія НЕ планується (HTML-прототипи
  в `prototypes/` робились лише для наочності дизайну — це специфікація, не кодова база).
- Архітектура з першого дня: чистий Dart-пакет **`stratum_core`** (тік-рушій, стан, баланс,
  офлайн-розрахунок) БЕЗ імпортів Flutter + окремий UI-пакет. Ядро має тестуватись без
  емулятора; симуляція «N тіків за мілісекунди» — інструмент перевірки кривих балансу.
- **Big numbers одразу**: порт break_infinity на Dart або власний тип мантиса+експонента.
  double ламається на 1e308, гра піде глибше. Міняти тип пізніше — заборонено собі.
- Баланс — у конфігу (JSON/Dart-константи одним модулем), не розсипаний по коду.
- Фонові навички власника: .NET/C#, Angular/TS, Flutter, PostgreSQL, Redis (бекенд, якщо
  знадобиться для клауд-сейвів/лідербордів — окреме майбутнє рішення).

## Що це за гра (одним абзацом)
Гравець — ШІ, що проводить симуляції видобутку. Ядро — нескінченне буріння вглиб планети А
циклами-тіками; два шари престижу (Перезапуск і Колапс); прогресія на комбінації онлайн+офлайн.
**Стовп «без клік-гринду» переформульовано 2026-08-26** (рішення власника): кліки існують,
але актив — ЗАВЖДИ бонус зверху, ніколи не гейт; пасив самодостатній. Див. «Драбина
автоматизації». Повний концепт: `docs/stratum-gdd-v1.md`. Дослідницька база жанру (формули
CIFI/ISEPS/CHAD, бенчмарки ретеншну): `docs/research-report.md`.

## Зафіксовані механіки (фінальні рішення)

### Тіки
- Тік — серцебиття гри: 4 с базово. ВСЕ (видобуток, крафт-черги, постачання
  сателітів, роли) відбувається на тіку. Нижня межа тіку 1 с; далі прокачка = дії за тік.
- **ФОРСАЖ СКАСОВАНО (рішення власника 2026-08-26), замість нього — ЕНЕРГІЯ і УДАРИ.**
  Форсаж був абстракцією («×2 темп» — цифра); удар по породі від кліка — відчуття.
  Уся машинерія передоплати/поновлення форсажу видалена. Машинерія перенесення прогресу
  тіка при зміні темпу ЛИШАЄТЬСЯ — знадобиться апгрейдам швидкості тіка.

### Драбина автоматизації (рішення від 2026-08-26)
**руки → бур (пасив) → авто-руки.** Гравець — ШІ, що поступово автоматизує сам себе.
- **Енергія** (колишня шкала заряду, кап 100, +1/2с власним циклом зі стопом на капі):
  паливо ручних УДАРІВ. Тап по породі = удар за 1 енергію. Утримання = автоповтор ударів
  (зручність пальця, не окрема механіка).
- **Шкода удару масштабується**: `max(база, k% від сили рига)` — інакше плаский удар на
  200 м стає шумом і актив помирає (урок Clicker Heroes).
- **ВСЯ здобич — з удару, і цикл бура кидає ТОЙ САМИЙ удар, що й клік (фінальне
  уточнення 2026-08-27).** Спільна лут-таблиця обох смуг, однакові шанси й обсяги:
  Реголіт завжди (рол у смузі ±20% навколо 35% циклової формули — це і є обіцяний
  min–max), Куприт/Ферит/Силіцит шансами таблиці, Кристали шансом, Квантоніум 2%
  (`strikeQuantoniumChance`) [2026-08-27: КВ ЛИШАЄТЬСЯ в луті, але поки БЕЗ
  застосування — скасована лише роль валюти перезапуску, див. «Дані, Перезапуск,
  Колапс»] — анти-brick крапля тепер ЖИВЕ в лут-таблиці, окремого
  циклового рола КВ немає (цикл котить її своїм ударом). Шкода циклу = сила бура + сила
  УДАРУ (уточнено 2026-08-27: удар циклу повноцінний — і шкода, і здобич, а не
  тільки здобич; крит множить весь блов). Кожна смуга — на власних
  RNG-потоках (`strike.*` проти голих імен; лут-КВ — `*quantonium.loot`).
  **Крит множить ЛИШЕ шкоду циклу, ніколи здобич** — що обіцяють картки здобичі, те
  будь-який удар і платить. Урок: подвійні джерела (окремий видобуток бура + лут,
  окрема крапля КВ + лут) читались гравцем як «×2» і «чому в бура частіше» — одна
  таблиця, одна правда.
- «Бур добуває свій специфікований ресурс» — ВІДКЛАДЕНО до типізованих бурів.
- **Типізовані бури (прийнято 2026-08-27, майбутнє):** окремий бур на кожен вид ресурсу
  (реголітовий — базовий; купритовий/феритовий/силіцитовий/кристалічний), відкриваються
  прокачкою дерева Перезапуску. Поки що екран «Бури» показує їх заблокованими картками.
- Кожен удар (ручний і бурів) трясе поточний шар (`hitShakes`) — коротка тряска з
  загасанням амплітуди, кожен удар рестартує її з повної.
- Навігація Видобутку: «Шахта» (копай руками + дивись на риги) і «Бури» (всі бури
  гравця, там же їх прокачка).
- **Бур — перша автоматизація**, купується за гроші з продажу перших ресурсів (фаза
  «старт без бура» — окремий крок реалізації). Авто-клікер — майбутня друга автоматизація;
  так, наприкінці драбини актив стає пасивом — це нормально; його баланс (реген проти
  витрати) — на момент появи.
- Запобіжники: енергія НІКОЛИ не блокує прогрес (пасив самодостатній; до першого бура —
  тюнити так, щоб покупка встигала раніше виснаження); ручна фаза — одноразова, після
  Перезапуску НЕ повертається (каскад розгону дає перший бур одразу); правило «ЖОДНОЇ
  стаміни на базові дії» тепер читається як «енергія гейтить лише БОНУСНИЙ актив».
- **Зміна темпу тіку НЕ скидає прогрес тіку (рішення 2026-08-26).** `TickScheduler`
  переносить накопичувач як ЧАСТКУ інтервалу, а не як секунди: тік, пройдений на 3/4,
  лишається пройденим на 3/4, коли інтервал ділиться навпіл. Саме масштабування (а не
  збереження секунд) закриває експлойт «накопичив на повільному темпі — вибив пачку тіків
  на швидкому»: у тіках накопичене число не змінюється. `TickEngine` перед зміною темпу
  банкує вже відпрацьований час (`syncNow`), інакше він губився б двічі, і перезаводить
  таймер на ЗАЛИШОК, а не на повний новий інтервал — інакше шкала стояла б повною і чекала.
- Дрейф колапс-порогу НЕ прив'язаний до тіків — тільки wall-clock (див. нижче).
- Офлайн = одна формула від Δt (скільки тіків минуло на офлайн-ефективності), без симуляції
  по кроках.

### Буріння: метри, HP шарів, товсті шари
- 1 шар = 1 метр. HP шару = щільність. За тік бур наносить шкоду = сила; надлишок переноситься
  на наступні шари (кап ~25 шарів/тік). Якщо сили < HP — шар пошкоджується частково,
  пошкодження зберігається (і між сесіями в межах рана).
- Щільність — прискорювана експонента + стрибок страти кожні 50 м (демо-числа в прототипі:
  5·1.055^м·2.5^(м/50); релізні — тюнити за docs/research-report.md розд. «Практичні висновки»).
- Кожен 25-й метр починає ТОВСТИЙ ШАР. **Уточнено 2026-08-26: товстий шар — це 3 метри
  породи одним куском.** Щільність ×10; за руйнування ГАРАНТОВАНО (без ролів шансу) глибина
  +3 м і всі ресурси ×3 від поточного добутку (руда ×3, квантоніум ×3) + XP. Товщина і
  множник — одне число (`PrototypeSimulation.thickSpan`), щоб їх не доводилось тримати в
  синхроні вручну. Метри всередині товстого шару не є окремими шарами: усе, що ходить по
  стеку (камера, рендер), мусить приземлятись на його початок (`layerStart`).
- Крит: один рол на цикл, база 5% ×2 (прокачується в Аватарі), застосовується і до шкоди,
  і до ресурсів. Понад 100% — суперкрити. Ехо-цикл: 1% на додатковий цикл, ланцюжки
  дозволені, кап прокачки 25–30%.

- **Структурна шкода (2026-08-27):** кожен удар (обидві смуги — цикл кидає той
  самий удар; ехо теж) додає до своєї сили `structuralShare × ЗАЛИШОК HP шару`
  (0.02% ПОПЕРЕДНЬО, конфіг). Саме від залишку, а не від повної щільності:
  кількість ударів до злому росте ЛОГАРИФМІЧНО від перевищення щільності над
  силою (перестрибнути свою глибину дорого, але скінченно: ×100 → ~100 ударів,
  ×10^6 → ~26.5к), фізично читається («у щебеню нема структури, якій падати»),
  і добиває шар завжди сила — структурний доданок лише асимптотично готує.
  Реалізовано доданком шкоди в момент удару, НЕ мутацією щільності: формула
  щільності лишається чистою, сейв не міняється (пошкодження вже зберігається).
  `hitsToBreak` рахує геометричне танення: n = ln(1 + r·HP/P) / −ln(1−r).
  Жодних жорстких стін: будь-який шар скінченний за конструкцією.

- **Темп видобутку (формула власника, 2026-08-27):**
  `ресурс/с = (енергія/с ÷ ціна_удару) × E[здобич за удар]
             + (E[здобич за цикл бура] + E[здобич за удар]) ÷ тривалість_циклу`,
  де `E[здобич за удар] = обсяг(глибина) × шанс`. Обидві смуги рахуються В УДАРАХ,
  бо цикл кидає той самий удар; доданок «здобич за цикл бура» поки ЗАВЖДИ 0 —
  окремого видобутку в бура немає, і саме сюди колись стануть типізовані бури
  (`expectedPerCycle`). Крит, ехо і виплати за пробиття СВІДОМО не входять: вони
  бонус зверху, тож число читається як підлога, а не як обіцянка, і не тремтить
  від удачі (перевірено тестом: замір іде на 2–5% ВИЩЕ за прогноз, ніколи нижче).
  Каденції передаються в ядро ззовні (`energyPerSecond`, `cycleSeconds`) — годинники
  належать застосунку. Показується у Складі рядком «X / с». УВАГА: це МИТТЄВИЙ темп
  на поточному вибої, а не сталий дохід — з глибиною все росте (реголіт 1.03^м).
- **ВИЯВЛЕНО 2026-08-27, не полагоджено:** ручна смуга ~40× потужніша за пасивну
  (енергія +20/2с = 10 ударів/с проти 0.25 удару/с у бура), бо здобич дає РІВНО ОДИН
  удар за цикл, і кількість бурів на дохід не впливає взагалі — тільки на шкоду.
  Це пряме порушення стовпа «актив — бонус зверху, пасив самодостатній».
  Варіанти: бур робить N ударів за цикл від кількості бурів / ручний удар дає частку
  здобичі / реген енергії дорожчає. Рішення за власником.

### Маніпулятор (рішення 2026-08-28) — ЗАМІНЮЄ «Удари»
**Чим б'є клік: рука-маніпулятор із тим самим буровим бітом, що несуть труби
рига.** Один шарнірний маніпулятор; тап = ШІ сам наводить руку і б'є. Обрано з
трьох варіантів (риг, вистрілений вручну / перкусійний молот / рука з бітом), бо
єдиний закриває лор без латок: «цикл кидає ТОЙ САМИЙ удар» — бо той самий біт;
`strikeShareOfRig` — бо один біт проти масиву з семи, ще й наведений вручну.
Вкладка перейменована «Удари» → «Маніпулятор».

- **Три ДЕТАЛІ, а не три статистики** (`ArmPart`): **Біт** (чим б'є), **Привід**
  (що вганяє біт у породу), **Живлення** (накопичувач у плечі). Гравець качає
  залізо, а не число. Кожна деталь несе КІЛЬКА бафів одночасно.
- **Бафи — таблиця** (`ui/arm_style.dart`, `armBuffs`): рядок = назва, крок за
  рівень, накопичене. Новий баф — новий рядок, і жоден інший файл про нього не
  чує. Рядок має `fromGeneration`.
- **Еволюція ВІДКРИВАЄ бафи.** 500 рівнів, 5 поколінь по 100 (Mk I–V).
  Заблокованого гравцю НЕ показуємо взагалі: баф просто з'являється в списку,
  коли покоління відкриється. Кожна сота — сюрприз, а не викреслений пункт,
  на який дивишся годинами.
- **Енергія переписана.** Старт 250 (було 100); рівень Живлення дає +10 до
  ємності (було +25). Тік регену завжди наливає ОДНУ одиницю, а рівень міняє
  ТЕМП таймера: `інтервал = 2.000 / (1 + 0.001 × рівень)`, тобто +0.1% за
  рівень і +50% на 500-му (2.000 с → 1.333 с). Прискорювати темп, а не
  скорочувати інтервал — єдине читання, що не перетинає нуль на вершині треку.
  Наслідок: стала швидкість руки = швидкість регену (0.5 → 0.75 удару/с проти
  0.25 у бура), а ЄМНІСТЬ чесно означає лише довжину ривка. Це заразом лікує
  перекіс 40:1, зафіксований 2026-08-27.
- **Смуга реголіту розтягується з двох кінців:** Біт піднімає МІНІМУМ
  (×1.03/рів.), Привід — МАКСИМУМ (×1.05/рів.). Рол тепер рівномірний у
  [min..max], а не навколо середнього — один той самий тяг, тож RNG-потоки не
  зсунулись. Привід також качає `pierceShare` (+0.001%/рів. до структурної
  шкоди).
- **Апгрейди руки і бурів платяться КРЕДИТАМИ (рішення власника 2026-08-30;
  було реголітом).** Перемкнуто всі шість платіжних точок (`canUpgrade` /
  `upgrade` / `affordableLevels` і бурові тріо), числа цін НЕ мінялись
  (ПОПЕРЕДНІ; у кредитах та сама цифра фактично дорожча — реголіт іде по
  0.4). `BuyButton` носить монету кредитів замість загальної іконки стосу.
  Мертва пара `buyDrill`/`buyPowerUpgrade` скасованої економіки лишилась на
  реголіті — її ніхто не викликає.
- **Купівля пачками**: `upgrade(part, levels:)` бере скільки влізе в гаманець і
  зупиняється на капі; `affordableLevels` рахує те саме наперед для кнопки
  «макс». 500 рівнів по одному — не рішення, а рутина.
- Сейв v7→v8 (2026-08-28): секція `data` СТИРАЄТЬСЯ — старі сирі й брутто були в
  нормованих спостереженнях (мільярди), і перенесення відкрило б колапс на першому
  кадрі та видало незароблені куби. Сейв v6→v7: піки деталей перебудовуються з
  `max(марка, generationOf(рівень))` — старіші білди тримали пік у РІВНЯХ, а
  пізніший клампив його до `lastMark`, через що 137 ставало «Mk V пройдено» і всі
  покоління читались як відкриті. Нова секція `bores` — треки бурів.
- Сейв v5→v6: секція `strikes` → `arm`; стара сила удару успадковується Бітом,
  стара ємність — Живленням, трек регену спадкоємця не має і зникає.
- **ЧИСЛА ПОПЕРЕДНІ, баланс пізніше** (рішення власника). Два спостереження,
  щоб не загубились: (1) «базова потужність +10/рів.» адитивна, а сила удару —
  `max(рука, риг×0.35)`, тож на глибині доданок рига накриває всі 500 рівнів
  Біта; (2) мін ×1.03 і макс ×1.05 компаундяться з різною швидкістю — на 500-му
  рівні розрив між кінцями смуги ×3e5, тобто удар стає лотереєю.

### Формула видобутку (правило власника 2026-08-30)
Для КОЖНОГО видобувного ресурсу кількість за видобуток рахується ТІЛЬКИ так:

    кількість = [базова кількість] × [множник всього видобутку] × [множник цього ресурсу]

- **Базова кількість** — чиста формула глибини/рола (oreDropAt, crystalDropAt,
  rawDataDropAt, смуга реголіту…), без жодних бонусів у ній самій.
- **Множник всього видобутку** — глобальні джерела (сьогодні:
  `fundGlobalScale` фінансування; завтра: вузли дерев, ранги — усе множиться
  сюди, а не вигадує собі нове місце у формулі).
- **Множник цього ресурсу** — персональні джерела смуги (сьогодні:
  `step^рівень` фінансування; завтра: типізовані бури, вузли на ресурс).
  У коді обидва праві множники вже живуть одним `fundScaleOf(id)`.
- Три наслідки-обов'язки: (1) множники застосовуються В ОДНОМУ місці кожної
  смуги (рол лут-таблиці, виплата товстого шару, `expectedPerStrike`) — не
  в кількох, що розійдуться; (2) кожна ВІТРИНА цитує добуток, а не голу
  базу — урок 2026-08-30: лут-таблиця шахти показувала 16.3 там, де удар
  платив 127.9, бо цитувала формулу без множників; (3) нове джерело бонусу
  зобов'язане влитись в один із двох множників, а не стати четвертим
  співмножником зі своїм життям.

### Ресурси і склад (рішення від 2026-08-26)
- Реєстр ресурсів живе в ядрі: `ResourceId` + `Stockpile` (`Signal<BigDouble>` на ресурс).
  Один плаский список, а не поле на ресурс: склад малюється обходом значень, тож
  сателітний матеріал колись додається одним записом, а не новим сигналом, вплетеним
  у кожне місце, що показує суму. Старі поля симуляції (`ore`, `quantonium`, `samples`…)
  тепер геттери-вью на реєстр.
- **Перелік видобувного ЗАТВЕРДЖЕНО (2026-08-26): 5 позицій, мова назв космічна.**
  Суфікс «-іт» = те, що копаєш; «-іум» = екзотика престижу (Квантоніум); Кристали — окремий тир.
  1. **Реголіт** — базовий, завжди, замість колишньої «руди» (сейви мігруються v1→v2:
     ключ `ore`→`regolith`). Майбутнє: обсяг [min..max] від покращень бура.
  2. **Куприт** (мідна руда) — шанс з 0 м.
  3. **Ферит** (залізна руда) — шанс з 50 м.
  4. **Силіцит** (кремнієвий мінерал) — шанс зі 100 м.
  5. **Кристали** — коштовний тир, без змін.
  Руди живуть ТАБЛИЦЕЮ (`oreTable`: id, потік RNG, глибина відкриття, шанс) — нова руда
  це рядок таблиці плюс свій підпотік, а не нова гілка коду. Шанси/обсяги ПОПЕРЕДНІ.
  Відрізане й відкладене (не викинуте): ільменіт/золото/кобальт/сірка/графіт/лід, тири
  кристалів, ланцюжок «руда→метал» через плавку (до розмови про крафт).
  **Гроші заведено 2026-08-27** як `ResourceId.credits` («Кредити», полиця валют,
  своя іконка): поки лише поняття — ні доходу, ні витрат. Далі: продаж видобутого
  (крім КВ і Зразків), покращення рига за кредити, скидання Перезапуском.
  **Вкладка «Торгівля» заведена 2026-08-29** у «Виробництві», ПЕРШОЮ в секції:
  продаж — це перше, що гравець робить із купою реголіту (продав → купив бур,
  що накопає наступну).

### Крафт (РЕАЛІЗОВАНО 2026-08-31; спека — docs/superpowers/specs/2026-08-31-craft-design.md)
Механіка — Forge з CHAD (реверс Forge Planner), «льодовиковість» виправлена
секундними базовими часами. УСІ ЧИСЛА ПОПЕРЕДНІ.
- **Ресурси**: Купрум/Ферум/Силіцій (матеріали, плавка руд + реголіт-флюс)
  і Дріт/Каркас/Чіп (продукція). Дві нові полиці складу; всі шість — у
  `priceTable` (метали ~+35% над сировиною, продукція ~+70%), групи
  Торгівлі «Матеріали»/«Продукція» розімкнено (`tradeGroups` у ядрі,
  по свічу на групу; `GroupCard` параметризовано; `trade_locked_group`
  видалено). Запити просять крафт самі — дошка бере sellable-склад.
- **Ядро**: `craftTable` (рецепт = рядок), `CraftLine` (Signal-стан:
  рецепт/tier/tierCap/speedLevel/limit/produced/rampSeconds + Computed:
  speedFactor, craftSeconds, unitsPerCraft, starving, ratePerSecond).
  Компресія ×2 вихід / ×3 входи / ×1.5 час за рівень, кап треку 14
  (×16384). `syncCraft(nowMs)` — безперервна конверсія формулою від Δt
  на ВИЗНАНОМУ годиннику (сам банкує `observeWall`; кап 48 год
  успадковано; пауза тане — свідомо, за правилом часозалежних механік).
  РОЗГІН: +25% швидкості за 10 хв безперервної роботи, інтеграл
  точний (кускова квадратика — паритет «один Δt == сума шматків»
  пришпилено), скидається ГОЛОДОМ і ПРОСТОЄМ, НЕ зміною рецепта.
  ДУБЛІКАТ 5% — вшитий у вихід МАТСПОДІВАННЯМ (конверсія неперервна,
  рол зламав би офлайн-паритет). Режим ∞ / N штук обирається З
  рецептом; після N лінія «виконано», рівень вільний. Компресію НЕ
  можна міняти під час роботи (`setCraftTier` відмовляє). Покупки за
  кредити: лінія 2000·6^n, кап 500·4^n, швидкість 300·1.6^n
  (прискорює темп, не ріже інтервал). Сейв — секція `craft`
  {last, lines[...]}, лише відхилення; невідомий рецепт плавиться до
  порожньої лінії (дух запобіжника №6). 16 тестів `craft_test.dart`.
  УВАГА тестам: епоха 0 — сентинель «не спостерігалось» визнаного
  годинника, стартуй час із ненульової мітки.
- **Офлайн**: лінії молотять 100% темпу; вироблене за відсутність
  вливається у вікно офлайн-доходу (`_settleAbsence` мержить результат
  `syncCraft` в OfflineGain).
- **UI (Л8 «Конвеєр», вибір власника з 8 намальованих лейаутів;
  канва: claude.ai/code/artifact/ef1dd1d6-...)**: `craft_screen` +
  `craft_line_card` (painter-конвеєр: кристали входів заїжджають,
  барабан крутиться, зливки їдуть; тікер через ValueNotifier у
  repaint, НЕ setState) + `craft_recipe_sheet` (рецепт І режим разом;
  цитує входи/час НА РІВНІ лінії) + `craft_resource_strip` (кредити +
  сировина + «СКЛАД») + `warehouse_sheet` (склад модалкою — той САМИЙ
  WarehouseScreen всередині HudModal, одна кімната — двоє дверей).
  Правила лейауту власника: висота картки СТАТИЧНА для всіх станів;
  стрічка ефектів — лише іконка+значення (три гліфи малюються
  `_FxGlyph`, у Tabler-наборі тріо немає); компресія на робочій лінії
  — замок, степер лише на зупиненій; режим на лінії — бейдж, не
  контрол; зміна рецепта — один тап по назві. Дрібні темпи (<10/с)
  форматуються `toStringAsFixed(2)` — стандартний формат BigDouble
  показував «+0 / с» на живій лінії. Іконки нових ресурсів — Ti
  (fallback ResourceIcon), беспоук-SVG додасть власник (ассет =
  повний перезапуск).
- Урок: `pokeListeners` з `initState` — assertion «setState during
  build»; перший синк робиться БЕЗ пока, білд і так прочитає свіже.
- **Друга ітерація (2026-08-31, вечір):** (1) СТОП/ПУСК на лінії —
  `halted` Signal (`setCraftHalted`): рецепт і замовлення лишаються,
  конверсія стоїть, рівень компресії вільний, розгін тане (простій є
  простій); кнопка пауза/плей у шапці картки, сейв-ключ 'h'. (2) Пікер:
  режим — степер `[-100][-10][-1][AUTO][+1][+10][+100]` (мінус нижче
  1 падає в AUTO, плюс із AUTO починає скінченне замовлення), і РІВЕНЬ
  КОМПРЕСІЇ обирається прямо в пікері — призначення НОВОГО замовлення
  законно ставить рівень (`assignCraftRecipe(tier:)`), midjob-лок
  стереже лише роботу; рядки рецептів цитують ціни й час на ОБРАНОМУ
  рівні і швидкості лінії. (3) «Швидкість не застосовується» виявилось
  ДВОМА подачами: анімація конвеєра не знала множника (тепер painter
  бере speed×ramp — стрічка реально пришвидшується) і формат темпу
  ковтав третій знак (0.147→0.149 читалось як «не змінилось») — темпи
  <1 тепер із трьома знаками. (4) Конвеєр — сюжет циклу: КУЧКА з
  кольорів усіх складових їде разом у низьку машину, зникає, і
  виїжджає ОДИН готовий продукт (а не два незалежні нескінченні
  потоки). Уроки: `crossAxisAlignment.stretch` у Row всередині
  необмеженої колонки = infinite height (IntrinsicHeight); епоха 0 —
  сентинель визнаного годинника.
- **Третя ітерація (2026-08-31, ніч):** пікер: інгредієнти лише
  іконка+BigDouble-число (FittedBox проти переливів), «РЕЖИМ» у
  фікс-коробці 18px (∞ нижчий за цифри — аркуш смикався); НОВИЙ якір
  `ModalAnchor.fill` — заповнює область МОНТУВАННЯ модалки (stretch
  віднімає хром оболонки, а Stack екрана вже всередині тіла —
  подвійне віднімання робило аркуш коротким); пікер і склад-модалка
  на fill. Конвеєр: `_ConveyorFreight` возить СПРАВЖНІ ResourceIcon,
  передані ззовні (беспоук-SVG поїде сам), подорож вантажу йде В ЧАСІ
  КРАФТУ (цикл = effectiveSeconds), всередині вищої машини —
  кросфейд складові→продукт у вікні; барабан = кільце прогресу
  ПОТОЧНОЇ одиниці з відліком часу під вікном (`craftProgress` =
  дробова частина producedCount/unitsPerCraft; UI екстраполює між
  секундними синками через знімок clockAt). Час у шапці —
  `effectiveSeconds` (трек × розгін). Бейдж скінченного замовлення —
  ЗВОРОТНИЙ відлік залишку (200, 197… 0), не X/Y. Пауза стоїть ПІСЛЯ
  «+X / с». Стрічка і вантаж їдуть в ОДИН бік (дашi бігли вліво —
  зсув віднімався замість додаватись). Урок hot reload: нові final-
  поля на старих інстансах = Null-not-double у paint — потрібен
  restart.

### Торгівля (реалізовано 2026-08-29; дизайн — prototypes/trade/)
Два зали, перемикач унизу екрана: **Продаж** і **Запити**. «Ціни» окремим
залом розглянуто й СКАСОВАНО: ціна вже написана в кожному рядку Продажу.
- **Ціни СТАЛІ за задумом** (`priceTable` у ядрі: реголіт 0.4, куприт 820,
  ферит 1400, силіцит 2900, кристали 12 — ПОПЕРЕДНІ): «коли продавати» — це
  питання про те, що гравцю треба, ніколи про таймінг. Ринок з коливаннями
  розглянутий і відхилений (RNG на доході = гейт, запобіжник №4).
- **Рядок позиції**: тумблер + частка (`sellShares` 25/50/75/100) + власна
  кнопка «ПРОДАТИ · +X». **Вимкнена позиція НЕ гасне** — тумблер значить рівно
  одне: «продати все» її пропускає; ручна кнопка — manual override і слухає
  палець, не прапор. «ПРОДАТИ ВСЕ» цитує ту саму суму, що плита «ЗА
  НАЛАШТУВАННЯМИ», тож кнопка ніколи не дивує.
- **Відображення ОДНЕ — групи** (рішення власника 2026-08-29; список був,
  прожив годину і видалений разом із перемикачем СПИСОК/ГРУПИ). Плитка групи:
  заголовок із ТУМБЛЕРОМ НА ВСЮ ГРУПУ (світиться, поки жива хоч одна позиція;
  тап — суцільний свіп, третього стану немає), налаштування обраної позиції
  ЗВЕРХУ, ряд іконок-членів ЗНИЗУ. Дві осі в гніздах ряду: ЗАЛИВКА каже, чия
  деталь розгорнута, ОБІДОК — чи йде вона в продаж (бурштин/сірий). «Матеріали»
  і «Продукція» стоять чесно замкненими до крафту.
- **Кнопка ручного продажу** забирає всю ширину після пікера частки
  (`Expanded`), текст у FittedBox: сума міняється щотіку, і кнопка, що дихає
  власним підписом, тягала б за собою пікер. Верхні плити зрізані ЛИШЕ згори:
  знизу під ними кімната, і зріз при підлозі читався б як картка, що плаває.
  Підписи-примітки («не продаються», «без штрафу») прибрані на запит власника.
- **Запити** (колишні «контракти», перейменовано власником): до `requestSlots`
  (3) на дошці, новий кожні 8 хв (`requestIntervalMs`; було 10, зменшено 2026-08-30), живе 12 хв
  (`requestLifetimeMs`), 1–2 позиції в запиті, обсяг = 15–45% ПОТОЧНОГО складу
  (запит ніколи не просить того, чого ран не бачив, і масштабується сам),
  премія +15–40% ПОВЕРХ прейскуранта. **Невиконаний зникає без штрафу** —
  премія лишається бонусом зверху. Слоти і премія — майбутні вузли дерева.
- **Запити — настінний час**, як дрейф: `syncRequests(nowMs)` жене дошку з
  батчу бура (Game) і з секундного таймера екрана; довга відсутність постить
  щонайбільше повну дошку, а не відтворює чергу. Свій RNG-потік
  `trade.request`. Картка запиту (ревізія 2026-08-29): інгредієнт — рядок
  «іконка · назва · є / треба» (бірюза/бурштин), і смуга запасу малюється
  ЛИШЕ коли її є про що питати (запас < треба, бурштинова): запити просять
  частку ПОТОЧНОГО складу, тож повні смуги на кожному рядку були декорацією,
  яка топила той один рядок, де справді нестача. Таймер — ЗАЛИВКА (Gauge), не
  комірки: час ллється, і в мові комірок годинник був третьою бірюзовою
  смугою поспіль; остання хвилина червона, цифри всередині. Заробіток і
  премія — плити HudStat швом упритул (зріз лише на нижньому лівому куті
  пари), кнопка «ВИКОНАТИ» третьою колонкою на висоті плит.
- Сейв: секція `trade` всередині рану (off/share/nextAt/requests), пишуться
  лише відхилення від дефолтів — старий сейв читається без міграції.
  Новий запит ще й ТОСТИТЬ нотисом у кутку («новий запит · премія +N%»,
  там же, де «автозбереження»); облік окремий від крапки — тост не гасить
  крапку, бо тост не є поглядом на дошку, а відновлена з сейву дошка не
  тостить (новина крапки, не тосту). 2026-08-30.
  Крапка уваги на «Торгівлі» = НОВИЙ (непобачений) запит, не «є що
  виконати»: виконуване тут майже завжди, і крапка-спроможність горіла б
  вічно. Той самий принцип 2026-08-29 переведено на «Бури»: стара умова
  читала СКАСОВАНУ економіку (canBuyDrill/canBuyPowerUpgrade мертвого
  upgrades_screen) і горіла назавжди; тепер крапка = трек, що СТАВ доступним
  з часу, коли гравець востаннє дивився на екран (знімок доступного при
  вході, `markDrillUpgradesSeen`). ПРАВИЛО ДЛЯ ВСІХ КРАПОК: крапка — це
  новина, а не спроможність. Крапка малюється і на комірці «ЗАПИТИ» самої
  смуги залів (`HudChoice.marked`) — ланцюжок секція → таб → комірка.
  Пришпилено `test/trade_test.dart` (продаж, дошка, сейв-раундтрип).
- **Другий видобувний ресурс — КРИСТАЛИ.** Один спільний мінерал БЕЗ прив'язки до страти
  (варіант «свій матеріал на кожну страту» розглянутий і відхилений). Падає шансом за цикл,
  як квантоніум: шанс `0.08 + 0.0012·м` з капом 40%, обсяг `1.02^м` — геометрично, щоб
  ресурс не відставав від породи, з якої його ріжуть. Множиться критом (руда теж; квантоніум
  ні). Свій підпотік RNG `'crystal'` — інакше нова механіка зсуває всі роли після неї.
  Товстий шар дає кристали ×3, як і решту.
- Повний перелік (2026-08-28): реголіт і руди, кристали, квантоніум (поки без
  застосування), **сирі дані** (шанс 2% з удару, полиця «дослідження»), зразки,
  OLAP-куби, очки колапсу, ФО; Капсули і Ядра скасовано. Полиці складу —
  видобуток / валюти / дослідження. Склад безмежний, капів немає (нічого не клампиться).
- ~~Склад — оверлей по тапу на верхню стрічку ресурсів~~ **(скасовано
  2026-08-30: склад — вкладка «Виробництва»)**. Оверлей тримався на тому, що
  стрічка була короткою формою складу; відколи стрічка стала шкалою
  Фінансування, підстава зникла разом із жестом.
- Презентація ресурсів (назви, іконки, кольори, полиці) — у застосунку
  (`ui/resource_style.dart`), не в ядрі: реєстр знає кількості й не має думки про іконки.

### Дані, Перезапуск, Колапс (переписано 2026-08-28 — ЗАМІНЮЄ вирахувану модель)
**Сирі дані — ВИКОПНИЙ ресурс, а не вирахуване число** (рішення власника
2026-08-28; скасовує модель від 2026-08-27, де дані рахувались із видобутку і
нормувались на типовий дроп). Лор: гравець копає ВСЕРЕДИНІ симуляції, і чим
глибше — тим ближче до її підкладки; уламок нескомпільованого світу можна взяти
в руки, а абстракцію «спостереження» — ні. Діра «навіщо симуляції руда для
перезапуску» була в КВАНТОНІУМІ (мінерал), а не тут.
- **`ResourceId.rawData`, шанс `rawDataChance` 2%, обсяг `1 + м/25`.** Шанс малий,
  але НЕ мікроскопічний: колапс-гейт — єдина стіна гри, і на тисячах ударів за
  сесію закон великих чисел тримає розкид у кілька відсотків. На 0.05% це була б
  лотерея, тобто порушення запобіжника №4.
- **Обсяг НЕ успадковує криву руди — це головне.** Дропи ростуть експонентою
  (реголіт 1.03^м), і дані на тій кривій робили удар на 400 м вартим тисяч ударів
  на поверхні, а фіксований поріг падав за хвилини (спостережено на живому сейві
  2026-08-27). Тому лінійно: на 400 м це 17, не тисячі. Пришпилено тестом, який
  перевіряє САМЕ інваріант — приріст 0→200 має дорівнювати 200→400.
- **`_information()` і нормування ВИДАЛЕНІ.** Замість них рядок лут-таблиці зі
  своїм потоком `rawdata` (тому роли, що йшли раніше, не зсунулись). Склад,
  стрічка, лут-таблиця, офлайн і «X / с» підхопили дані самі, без нового поля.
- **OLAP-куби** — банкована валюта (назва обрана 2026-08-28 з-поміж Корпус /
  Індекс / Дистилят / Тензор / Артефакт). Обрано за форму: куб можна намалювати і
  покласти в стійку, а гра весь час робить числа видимим залізом. «Корпус» відпав
  тим, що на екрані з Датацентром читається як будівля; «Кристал даних» — бо
  Кристали вже видобувне.
- **`куби = сирі_рана × compileRate / rawPerCube`** (`rawPerCube` = 1000,
  `compileRate` = 1 + 0.05×рівень). **Ділення, не степінь** — і рахується від
  РАНА, не від брутто циклу (рішення власника 2026-08-28).
  Чому саме ділення: при показнику <1 дроблення платить `n^(1-показник)` разів
  більше, тобто на 0.25 сотня коротких ранів дає ×31 проти одного довгого, і
  оптимумом стає «перезапуск щодві хвилини». Ділення НЕЙТРАЛЬНЕ до дроблення:
  та сама підкладка дає ті самі куби хоч одним раном, хоч сотнею, тож питання
  «коли перезапускатись» знову про те, коли хочеться витратити.
  Різницеве банкування від `брутто^ступінь` СКАСОВАНЕ разом зі степенем;
  `cycleData` лишився як статистика циклу («за цикл»), а не як база виплати.
- **Ієрархія лишається: Цикл → симуляції.** Симуляція завершується Перезапуском,
  цикл — Колапсом.
- **Перезапуск без порогу**, єдиний гейт — симуляція мусить прожити ~2 хв
  МОНОТОННОГО ігрового часу (ПОПЕРЕДНЬО, конфіг).
- **Колапс міряється в КУБАХ, і кожен сервер — один колапс** (рішення
  2026-08-28). `collapseReady` = є хоча б один повний сервер; шкала і прев'ю
  Перезапуску читають одне й те саме число, а не дві різні валюти.
  Наслідок, який треба тримати в голові: покращення компілятора піднімає куби,
  тобто НАБЛИЖАЄ колапс — один важіль, два ефекти.
- **Драбина серверів — накопичувальна геометрична:**
  `вартість(сервер n) = поріг × 2.6^n`, `maxPendingCollapses = 5`.
  Сервер 2 набирається ВІД місця, де скінчився перший, тож стіна читається
  зліва направо без розривів (`rackFill`). При базі 150k це 150k / 390k /
  1.01M / 2.64M / 6.85M — рівно та форма, яку задав власник прикладом.
  Числа були «з голови», формула — ось вона; крутити треба 2.6.
- **Скільки серверів повних, стільки очок колапсу і стільки закритих циклів.**
  Забрати три разом законно: +3 очки, +3 цикли. Поріг росте ×4 за КОЖЕН
  колапс, тож пачка з трьох дає ×64 до наступного порогу — сумарне зростання
  однакове хоч по одному, хоч пачкою, але стрибок різкий і має бути в прев'ю.
- **Тримати стіну повною — програшно.** Понад п'ять колапсів складати нікуди,
  тож «залишу на потім» перестає бути безпечним простоєм.
- **Циклів на старті НУЛЬ.** `cycleNumber` = зроблені колапси, а не порядковий
  номер поточного; підпис «ЦИКЛІВ», бо «Цикл 0» читалося б як номер.
- `поріг = база × 4^колапсів × 0.97^діб`. **База 1.5e5 кубів**
  (перерахована 2026-08-28: старі 3e8 були в нормованих спостереженнях, яких
  більше не існує). Заміряно `tool/data_pace.dart` — він рахував ТІЛЬКИ смугу
  бура і занижував утричі, полагоджено: тепер бере `yieldPerSecond` обох смуг
  і рахує в кубах.
  ДРЕЙФ живе тут: −3%/добу настінного часу, неперервно формулою від Δt,
  однаково онлайн і офлайн; на новому циклі заново від бази.
  **КАП ДРЕЙФУ — 30 діб (рішення власника 2026-08-29; раніше капу не було).**
  На 30-й добі стіна стоїть на 0.97^30 = 40% бази, тобто полегшення ×2.5 —
  МЕНШЕ за ×4, які додає один колапс, тож чекання ніколи не обжене драбину.
  Без капу воно колись обганяло б, а гравець, що зник на сезон, повертався б
  до порогу, який розтанув у ніщо. 15 діб розглядались і відкинуті: ×1.58 —
  замало, щоб зрушити з місця того, заради кого дрейф і існує.
  `driftDays` / `driftProgress` / `driftDiscount` — усе, що потрібно екрану;
  пришпилено `test/collapse_drift_test.dart`, і серед тестів є САМЕ той, що
  стереже інваріант «вікно дрейфу дешевше за один крок драбини».
- **Очко колапсу**: +1 за КОЖЕН забраний сервер, у мета-дерево циклу.
- **Добровільний рестарт циклу** — будь-коли, весь прогрес циклу згорає, нового
  очка немає, очки колапсу зберігаються і саме тут перерозподіляються.
- Перезапуск скидає: глибину, ресурси (СИРІ ДАНІ ТЕЖ — вони ресурс), бури,
  модулі, крафт. Переживають: Аватар, куби, дерево симуляції, Зразки, ФО,
  сателіти, `cycleData`. Колапс і рестарт циклу додатково стирають куби і дерево.
- Прев'ю завжди видиме: на Перезапуску «+X · з цієї симуляції», на Колапсі
  «перенасичення X% · поріг Y · дрейф −3%/добу».
- **ВІДКРИТЕ:** площа бура множить і дані теж (ширший захват = більше підкладки за
  удар). Логічно, але це важіль на колапс-поріг — рішення, чи нормувати.

### Фінансування (рішення власника 2026-08-30; назви обрані з варіантів)
Рівнева мета-система від ОБОРОТУ: `creditsEarned` (сумарний дохід симуляції;
витрати його не зменшують) закриває **РАУНДИ** — лор: ШІ, що доводить
прибутковість, отримує від замовника наступний раунд.
- **Драбина**: раунд n коштує `roundCostBase(500)·1.9^n`; рівень зчитується
  ЛОГАРИФМОМ (`financeRound`), не циклом — оборот у qa-масштабах зациклив би
  лічилку. ЧИСЛА ПОПЕРЕДНІ.
- **v2 (рішення власника 2026-08-30; «статті» v1 прожили день).**
  **ПРАВИЛО ТАБЛИЦІ: фінансування качає те, що ЗАРОБЛЯЄ кредити** — таблиця
  = прейскурант + самі кредити. Сирі дані і КВ — паливо престижу: їхні
  множники підніматиме дерево/прошивка, і на них не їде НАВІТЬ глобальний
  множник. Заразом **силіцит і кристали зняті з продажу** — прейскурант:
  реголіт 0.4 / куприт 820 / ферит 1400; `fundTable` 5 рядів: Кредити
  ×1.07/рів., Реголіт ×1.05, Куприт ×1.06, Ферит ×1.05 і — ЄДИНИЙ виняток
  власника з правила — Сирі дані ×1.01 (підкладка і є продукт, заради якого
  копають; найповільніший крок). КВ лишається деревам. Кроки НЕРІВНІ
  навмисно, числа власника, ПОПЕРЕДНІ; компаундяться множенням.
- **ТРАНШІ: 3 за раунд** (`tranchesPerRound`; майбутній вузол дерева
  підніме). Назад не повертаються. «Вільні» — ПОХІДНЕ число
  (раунди×3 − витрачені), не валюта в сейві: транш неможливо загубити, а
  майбутній вузол «+1/раунд» доплатить заднім числом за ВСІ закриті раунди —
  тримати в голові при балансі вузла.
- **ЦІНА РІВНЯ — СХОДИНКАМИ (власник, 2026-08-30)**: рівні 0–19 коштують
  1 транш, 20–39 — 2, 40–59 — 3… (`investCostAt = 1 + рівень÷20`).
  «Витрачено» рахується ЗВАЖЕНО за цінами (`tranchesInto`, замкнена
  формула) — тож глибокий рівень і дорожчий, і сам себе компенсує: пампує
  глобальний ×1.01 стільки разів, скільки коштував. Кнопка інвестиції несе
  ціну («+РІВЕНЬ · 2 ТРАНШІ»), бо «+1 транш» на дорогому рівні брехала б.
- **Гачок подарунків: `grantFundLevels(id, levels)` + `tranchesGranted`**
  (2026-08-30). Майбутній вузол дерева «безкоштовно +10 рівнів» = один
  виклик: подаровані рівні лізуть у ранги і глобальний ЯК ВИТРАЧЕНІ транші
  (очікування власника), але їхня ціна кредитується в `tranchesGranted`,
  інакше «витрачено» мовчки з'їло б ВЛАСНІ вільні транші гравця — подарунок,
  за який платиш. Кап поважається, поле в сейві, пришпилено тестом.
- **РАНГИ — з ВИТРАЧЕНИХ траншів** (інвестор винагороджує вкладення, не
  накопичення): пороги 20/45/75/110/150…, крок 20→25→30→35→40 і ДАЛІ ЗАВЖДИ
  40 — стеля кроку і є фікс софт-лока. **Лок був справжнім ДВІЧІ**:
  квадратичні пороги проти лінійної місткості замикали на ранзі ~10; після
  звуження таблиці до 4 рядів при капі +5 — уже на ~4. Тому кап рівнів
  **+10 за ранг** (не +5 з ТЗ; пороги власника збережені точно), база 10.
  Інваріант «пороги ніколи не обганяють місткість» пришпилено тестом до
  рангу 200, запас 50 траншів назавжди.
- **Глобальний множник**: ×1.01 за кожен ВИТРАЧЕНИЙ транш і ×1.02 за кожен
  ранг; множить усі 4 смуги таблиці (і лише їх). Петля «кредити → оборот →
  раунди → транші» свідома, її стримує логарифм драбини раундів.
- Множники вшиті в ОДНОМУ місці кожної смуги (`fundScaleOf(id)`):
  лут-таблиця, виплати товстого шару, `expectedPerStrike`, продажі й запити
  — «X / с», офлайн і прогнози чесні автоматично. Пришпилено
  `financing_test.dart` (11 тестів, з анти-лок інваріантом).
- Аркуш НА ВЕСЬ ЕКРАН (вибір власника): плити обороту/траншів, смуга
  раунду, ранг-блок (витрачені транші, глобальний множник), 4 ряди ресурсів
  (крок, рівень/стеля, свій множник, «+1 ТРАНШ» / «СТЕЛЯ»).
- **Показання смуги раунду — КУМУЛЯТИВНА пара** «оборот / сумарний поріг
  наступного раунду» (вибір власника після двох ітерацій): пара «в межах
  раунду» поруч із плитою довічного обороту читалась як «вже перевиконано»,
  а залишок «ще N» власник відхилив на користь X / Y на одній шкалі з
  оборотом. Заливка лишається чáстковою в межах раунду.
- **Верхня смужка = фінансування (2026-08-30), ресурси з неї ЗНЯТІ.**
  Бейдж «Р7» СИДИТЬ НА прогрес-барі (бар починається з-за нього), читання
  «X / Y ▣кр» всередині бару, крапка на бейджі = вільний транш. ТАП ПО ВСІЙ
  СМУЖЦІ відкриває аркуш «ФІНАНСУВАННЯ» (оборот, транші, три статті з
  «+1 ТРАНШ»); шеврон складу зник.
- **Склад через це став ВКЛАДКОЮ** — першою в «Виробництві» (`WarehouseScreen`,
  нова іконка nav_warehouse.svg): оверлей тримався на тому, що «стрічка — його
  коротка форма», а стрічка тепер коротка форма фінансування. За тим самим
  правилом.
- **Шкала-погоня** (3 ітерації 2026-08-30, кожна ховала ефект по-своєму):
  (1) смужка чула про продаж лише з батчем бура — тепер акт продажу штурхає
  `pokeListeners` одразу; (2) комірки ковтали дельту, меншу за комірку —
  заливка суцільна (`Gauge`+`Gauge.live`, білий шар прибулого під
  кредитно-зеленим, що доганяє); (3) **`animateTo` БЕЗ явного duration
  масштабує тривалість контролера під залишок відстані** — малий продаж
  отримував погоню у 83 мс (заміряно лог-файлом, не вгадано) і читався як
  стрибок. Явний `duration: 900ms` у самому виклику. Контраст різниці —
  майже білий проти зеленого: два світло-зелені на смузі 14 px читаються як
  один колір (теж перевірено кадром).
- `Game.pokeListeners()` — публічний штурхан для UI-актів, яких не бачить
  жоден сигнал складу. Сейв: секція `finance` {earned, статті≠0}.
  Перезапуск МАЄ скидати фінансування (як кредити) — гачок на майбутній акт.

### Датацентр (рішення 2026-08-28; переїхав із головного 2026-08-29)
Єдине місце в грі НЕ під землею: шахта, рука і бури дивляться в діру, а
Датацентр — на машину, заради якої діру копають. Уламки підкладки піднімаються
з гирла шахти в підлозі й осідають у стійках. **Живе на вкладці «Симуляція»
секції «Датацентр»**, а не на головному екрані — див. наступний розділ.
- **У грі дрейф зветься «НАГРІВ»** (рішення власника 2026-08-29): «дрейф» —
  слово з астрономічного словника «Колапсу», а кімната тепер говорить машиною
  (стійки, перевантаження, прошивка, патчі). Нагрів заразом ЄДИНИЙ пояснює
  кап: кімната виходить на сталу температуру й більше не гріється, тому на
  капі рядок читає «рівновага». У коді лишається `drift` — те саме правило,
  що й із Колапсом.
- **Нагрів видно смугою НАД стіною** (2026-08-29): `HudProgress` на всю ширину,
  читання «−2.4% · 0.8 / 30 дн», а на капі «ДРЕЙФ ВИЧЕРПАНО». Комірки, а не
  заливка, бо дрейф — це трек, який цикл ПЕРЕТИНАЄ (скільки діб полегшення вже
  витрачено), а не рівень, що ллється. Стоїть над стійками, бо це те, на чому
  вони стоять: із кожної ціни під ним це вже відняли.
- **Кожна стійка — один колапс.** Прейскурант із підписами під КОЖНОЮ стійкою
  скасовано 2026-08-29: відколи ладдер замкнений після першої, п'ять підписів
  були чотирма порожнечами і однією цифрою збоку. Тепер ОДИН рядок під стіною —
  `НАСТУПНА СТІЙКА · тримаєш / треба ▣` — тією ж формою, що й заголовки актів
  над ним (назва, волосяна лінія, число), і тією ж парою «X / Y», що й решта
  прогресів гри. Іконка куба і є одиницею, тож число не потребує підпису;
  чисельник — це ТОЙ САМИЙ `walletEarned`, який рядком вище обіцяє Перезапуск,
  і показати його двічі — єдиний спосіб сказати, що купа одна, а вжитків два.
  Коли всі відкриті стійки повні, рядок каже «СТІНА ПОВНА» і не називає ціни
  ємності, якої ще немає. `ServerRack` більше не має параметра `cost`.
- **Стан живе в лампах, не в слотах** (рішення власника 2026-08-28). Слоти
  малюються ЗАВЖДИ й однаково; лампа зелена, поки є місце, червона на
  витраченому слоті, жовта на тому, що набирається (і його лінія залита на
  частку). Так перенасичення читається як ВТРАТА ємності, чим воно і є, а не
  як здобутий прогрес. Витрачена лампа рівна, вільна тихо дихає, та, що
  набирається, пульсує найсильніше — око знаходить фронт без підказки.
  Червоний заведено в палітру як `Palette.alarm` (#E8706B) — єдиний у грі.
- Темп появи уламків береться з `yieldPerSecond(rawData)` — з того самого числа,
  що годує «X / с» у Складі.
- Розмітка як усюди: смуга від краю до краю, далі плаский список із волосяними
  лініями. Жодних карток. **Розчинення тут НЕМАЄ і бути не може** — урок
  2026-08-29: прийом скопіювався з Шахти й Маніпулятора, де смуга має власне
  тло (порода) і градієнт його гасить. Тут гасити нічого, стійки стоять на полі
  оболонки, і градієнт малював непрозору смугу впоперек екрана — три фони
  замість одного. Ніщо в цій смузі не має права бути непрозорим: підлога — лінія
  і світіння, не заливка.
- Перезапуск і Колапс живуть тут, поки з прев'ю і міткою «згодом»: самих актів у
  ядрі ще немає, а кнопка, що не працює, гірша за число, яке чесно чекає.

### Зразки
- Даються за ПЕРШЕ руйнування товстого шару в межах ЦИКЛУ (не за кожен метр).
- Колапс скидає позначки → новий цикл заново роздає Зразки. Купують приємні перманенти,
  НЕ гейтять ядро прогресії.

### Дерево симуляції (валюта — OLAP-куби; «за Капсули» скасовано 2026-08-27)
- ГРАФ, не лінійний список: вузли з ребрами, вимоги по батьках, є вузол-ромб зі злиттям
  двох гілок (Знижка: Потужність lv3 ТА Збагачення lv3). Референс — Stratum.dc.html.
- Структура нод: repeatables (exp 1.08–1.32, 100–200 рівнів) + one-shot гейти з великими
  фіксованими цінами (сателіти, механіки).
- Знижки: глобальна купується ЗА OLAP-куби (до ~40%; час її НЕ дає) + модульна «−0.5% за
  побудований рівень конструкції, кап 20%». Стакання МНОЖЕННЯМ: ціна×(1−г)×(1−м). Перша
  конструкція типу — завжди повна ціна.

### Бури (рішення 2026-08-28) — інтерфейс і треки
Навігація: **список бурів → екран конкретного бура** (обрано з трьох варіантів;
третій шар навігації відхилено — він з'їдав 126 px низу телефона і 40 px острова
саме там, де живуть покращення). Список — п'ять рядків без рамок із чотирма
числами, за якими бури порівнюють; замкнені приглушені, тобто список водночас і
карта того, що попереду.
- **Кожен бур видобуває свій ресурс.** Таблиця `drillTable`: новий бур — рядок,
  не гілка коду.
- **Площа: `видобуток = щільність × πr²`**, база r = 5 м, +1 м за рівень.
  Приріст від +1 м = π(2r+1), тобто росте ЛІНІЙНО з рівнем: при r 5 це 35, при
  r 100 — 631 (×18). Наслідок, який треба тримати в голові: сумарний видобуток
  росте КВАДРАТИЧНО, тобто радіус — ручка ДОХОДУ, і глибинну експоненту він
  ніколи не наздожене. Реалізовано множником на здобич ТОГО САМОГО удару
  (`drillYieldScale`), а не другим джерелом — правило «одна таблиця, одна
  правда» лишається. На рівні 0 множник рівно 1.0.
- **Швидкість: `інтервал = max(2 с, T₀ × 0.99^n)`** (числа власника).
  ДВА наслідки, яких у формулі не видно: (1) **трек скінченний** —
  `0.99^n = 0.02` при n = 389.2, отже 390-й рівень упирається, а далі рівні не
  роблять НІЧОГО; `drillDriveCap` рахує це з формули, і мертві рівні не
  продаються; (2) **цінність усього треку задає СТЕЛЯ, не відсоток**:
  `T₀ / стеля`. Звідси правило: **типізований бур мусить стартувати повільним** —
  при T₀ 100 с трек вартий ×50, при 4 с — ×2 і продавати нема чого. Реголітовий
  бур має T₀ = 4 с (він серцебиття гри), типізовані — 100 с.
- **Калібрування**: крит і ехо ЦЬОГО бура одним рівнем. `echoChance` більше не
  константа — читається з реголітового бура.
- Ціни ПОПЕРЕДНІ: 400 / 900 / 2500, ріст 1.15 / 1.17 / 1.22.
- Анімація бура — мовою маніпулятора, тими самими painter'ами рига. На вибої
  намальоване **коло охоплення** кольором ресурсу: радіус — головне число бура і
  єдине, якому інакше нічим себе показати.
- **ВІДКРИТЕ І НАВМИСНО ВІДКЛАДЕНЕ: хто і як наносить шкоду шару.** Розібрано
  чотири моделі (аркуш «Модель» на полотні): A — бор веде глибину, типізовані
  лише збирають (рекомендовано: крива щільності зтюнена під ОДНЕ число);
  B — сума всіх бурів (щільність доведеться тюнити під кількість бурів);
  C — тиск `сила / площа^k` (єдина, що дає справжній вибір білда, але це
  покращення з мінусом); D — кожен гризе свою частку шару (шар перестає бути
  одним HP — найдорожча). Обмеження, спільне для всіх: HP шару росте
  експонентою, тож ручка, що веде ГЛИБИНУ, зобов'язана бути експоненційною за
  рівень; адитивні й квадратичні ручки годяться лише для доходу.
  Поки рішення немає — шкода рахується як раніше, треки вже стоять.

### Рамка в рамці (2026-08-29)
Коли Датацентр став екраном із власною рамкою, консоль актів усередині нього
малювала ЩЕ чотири кутові удари — і нижня пара опинялась за кілька пікселів від
рамки екрана. Ліки: `HudBrackets.struck` б'є лише ВЕРХНЮ пару. Блок і далі
каже, де починається консоль, але внизу відкритий і витікає в підлогу екрана
замість того, щоб обгородитись. Правило, яке з цього виросло: **вкладений блок
не повторює кути того, що його вміщає — він домальовує те, чого тому бракує.**

### Навігація: секція «Датацентр» (рішення власника 2026-08-29)
П'ята вкладка нижньої навігації, чотири підпункти. Головний екран при цьому
ЗВІЛЬНИВСЯ і знову тримає лише назву гри над дрейфуючим полем: місце без
роботи — це те, що робить вихід із секції виходом, а не приземленням на ще
один екран.
- Нижній ряд секцій (самі іконки) — ЦЕНТРОВАНИЙ кластер по 64 px на таб:
  78 був кластером, поки секцій було чотири; на п'ятьох він заповнював ряд
  впритул і читався як розкиданість (виправлено 2026-08-29).
- **Симуляція** — обидва акти, стіна стійок і показники рана (циклів,
  симуляцій, сирі дані, куби, глибина). Крапка уваги на вкладці світиться,
  коли є що забрати (`screenNeedsAttention`).
- **Дерево** — куби. Назване коротко: смуга підкатегорій уже сказала, чиє воно,
  а «Дерево симуляції» в ряду з чотирьох з'їдало сусідів.
- **Прошивка** — патчі.
- **Аналітика** — живі числа рана (темп, прогноз наступної стійки, дрейф). Поки
  заглушка; «Статистика» в Консолі лишається підсумками за ВЕСЬ час, і саме це
  розводить два екрани чисел: один про зараз, інший про взагалі.
- `GameScreen.tree` переїхав із «Дослідження» — там лишились Аватар і Зразки.
- **Дерева були модалками рівно один день.** Вкладка і модалка на одну кімнату —
  це двоє дверей, які довелось би вічно тримати в синхроні. `TreeSheet`
  видалено; `tree_screen.dart` тримає `TreeKind` (валюта, іконка, колір) і ОДИН
  екран на обидва дерева — вони той самий об'єкт на двох рівнях.
- Кнопки «ДЕРЕВО СИМУЛЯЦІЇ» / «ПРОШИВКА» з актів прибрані: кнопка, що лише
  перемикає вкладку, — це друга назва того, що вже каже навігація.
- **Рамка екрана дерева — кольором його валюти** (`_framed` в оболонці): межа
  каже, який гаманець на кону, ще до першого намальованого вузла.
- Іконка секції — нова `nav_datacentre.svg` (вузька шафа зі слотами й лампами).
  Додавання ассета = ПОВНИЙ перезапуск `flutter run`, hot reload маніфест не
  підхоплює.

### Назви (рішення власника 2026-08-29)
- **«Очки колапсу» → «Патчі».** Пропонувався «Коміт», і власник зняв його
  правильним аргументом: прошивку можна перебудувати між колапсами, а коміт —
  запис, який не забирають. Патч якраз накладають і знімають.
- **Кнопки актів названі за ВИПЛАТОЮ, а не за втратою** (2026-08-29):
  «ЗГОРНУТИ» → **«СКОМПІЛЮВАТИ»**, «ЗАБРАТИ» → **«ПРОШИТИ»** (з `×N`, коли
  готова не одна стійка). Обидві — дії над кодом на сусідніх рівнях: ран
  компілюється в куби (це вже власне слово моделі — `compileRate`,
  `rawPerCube`), цикл прошивається в самого ШІ. Кожна кнопка веде рівно в ту
  вкладку, де її здобуток витрачається.
- **Акт «Колапс» у грі зветься «Перевантаження»**: стіна стійок показує саме
  перенасичення, і назва тепер описує те, що видно. У КОДІ й у решті цього
  журналу механіка лишається `collapse` / «Колапс» — перейменування
  ідентифікаторів і ключів сейву коштувало б міграції заради слова.

### Один клас — один файл (рефакторинг 2026-08-30, правило власника)
**У файлі живе ОДИН публічний клас/віджет** плюс його власні одноразові
приватні листки (State, painter, приватний рядок списку, який ніхто інший
не бачить). Багатокласові файли розрізано:
- `ui/hud.dart` (23 класи) → тека `ui/hud/` по файлу на віджет; сам
  `hud.dart` став БАРЕЛЕМ з export'ів, тож імпорти гри не змінились.
  `_CornerClipper` став публічним `CornerClipper` у `hud/corners.dart`
  (він потрібен і `HudTap`, і `HudChoice`).
- `ui/game_shell.dart` (19) → `ui/shell/` (resource_bar, round_gauge,
  round_badge, since_clock, background_overlay, pause_overlay,
  breach_overlay, nav_bar, dotted, screen_strip, screen_placeholder);
  приватні класи промоуті в публічні.
- `ui/drill/rig.dart` (14) → drill_string / energy_plate / energy_meter /
  pipe / bit / strike_zone; `rock.dart` → rock / hit_shake / layer_tile /
  stone_painter / crack_painter / noise; `overlays.dart` → depth_readout /
  float_layer / flash; `deck.dart` → deck / loot_table / deck_handle.
  rig.dart і overlays.dart ВИДАЛЕНІ, імпортери переведені на конкретні
  файли (діаграми беруть лише painter'и, не віджети).
- Екрани віддали свої картки: part_card (+blow_summary) зі StrikesScreen,
  track_row із DrillDetail, trade_group_card / trade_locked_group /
  trade_request_card із TradeScreen, save_slot_card із SaveMenu,
  cubes_icon / resource_icon із resource_style, mark_glyph / part_face із
  part_glyph. Моделі `Notice`/`NoticeKind` і `FloatingNumber` виїхали з
  `game.dart` у власні файли; `ui/floating_number.dart` перейменовано в
  `floating_number_view.dart` (файл зветься за класом).
- Ядро: support-типи (`StrikeOutcome`, `CycleOutcome`, `OfflineGain`,
  `ArmPart`, `DrillPart`, `DrillId`, `DrillRow`, `DrillState`,
  `TradeRequest`) виїхали з `prototype_simulation.dart` у власні файли
  `src/preview/`, барель `stratum_core.dart` експортує кожен.
- СВІДОМО НЕ розрізане (когезивні сім'ї, а не «різні класи»):
  `tokens.dart` (Palette/Strata/AppMetrics/AppText — токени однієї мови),
  `gauge.dart` (Gauge+ProgressPainter — обгортка і її двигун),
  `reactive_graph`, `tick_engine`, `tick_scheduler`, `number_style`,
  `save_codec`, `random_source`, `balance_harness`, `stockpile` — кожен
  файл і є одна підсистема.

### Дублікати, злиті 2026-08-30 (той самий рефакторинг)
- `_Rule` жив у П'ЯТИ файлах → `HudRule` (`hud/rule.dart`).
- `_Dissolve` ×2 → `ui/dissolve.dart`.
- `_BatchPicker` ×2 → `ui/batch_picker.dart`; версія drill_detail була
  СТАРІША (рукописний ряд зі скругленнями) — обидва екрани тепер на
  HudChoice, тобто пікер бурів заразом перейшов на мову пульта.
- `_BuyButton`/`_Buy` (однакове тіло, різні виклики sim) →
  `ui/buy_button.dart` (`BuyButton(cost, enabled, onTap)`) — цінова
  кнопка більше не знає про sim, їй дають готові факти.
- `_FlatButton`/`_Flat` — обидва були обгорткою `HudButton(onTap: null)`;
  класи видалені, виклик інлайновий.
- **`upgrades_screen.dart` ВИДАЛЕНО як мертвий**: UpgradesScreen /
  UpgradeRow / PressButton не імпортував ніхто (економіка екрана
  скасована ще журналом 2026-08-29).

### Спільні компоненти UI (рішення 2026-08-28)
- **`GameModal`** — одна модалка на гру: скрим, панель, шапка з іконкою, назвою,
  необов'язковим числом біля назви і хрестиком. Параметри: `anchor`
  (`centre` / `bottom` / `stretch`), `accent`, `inset`, `contentPadding`,
  `trailing`, `footer`. На неї переведені ВСІ шість: Консоль, сходи Mk, Склад,
  Збереження, Офлайн-дохід, Еволюція. До того скрим був трьох різних щільностей
  (0x99 / 0xB3 / 0xCC), а шапка — чотирьох діалектів.
- **`Stat`** тепер уміє дві додаткові форми: заголовок БЕЗ числа (секція — це
  readout, якому нема про що звітувати) і число БЕЗ заголовка (склад, де назва
  вже написана поруч). Плюс властивість `rule` — волосяна лінія від заголовка до
  краю, кольором заголовка з альфою 0.15.
- **`ResourcePlate`** — плитка ресурсу, спільна для лут-таблиці шахти і полиць
  складу: назва кольором ресурсу, дрібний факт біля неї (шанс у шахті, підказка
  на складі), іконка з числом. Прапорець `shadows` — підйом тексту потрібен над
  породою, на панелі ні.
- **`Gauge` / `ProgressPainter`** (2026-08-29) — одна смуга на гру. Було
  чотири: painter енергії, шкала колапсу, трек Mk і пошкодження шару, вже
  розійшлись у чотирьох радіусах, трьох кольорах доріжки і двох способах
  малювати ту саму заливку. Один painter (`value`, `track`, `fill`/`gradient`,
  `radius`, `minSliver`) плюс обгортка з `height` і `label` поверх.
  Два конструктори під два реальні випадки: `Gauge(fraction:)` для статичної
  (загорнута в `AlwaysStoppedAnimation`, щоб painter скрізь бачив один тип) і
  `Gauge.live(value:)` для покадрової — listenable іде прямо в `repaint`, тож
  енергія коштує перемальовку, а не перебудову дерева.
  Стоковий `LinearProgressIndicator` НЕ підходить: він у Material, якого
  застосунок не імпортує ніде, і тягне Theme зі своїми кольорами й кривою
  анімації заради смуги в 4 px.
  Градієнт натягується на ВСЮ смугу, не на залиту частину — інакше колір
  міняється, поки смуга росте.
- **`ServerRack`** (2026-08-29) — стійка як самостійний віджет, а не шматок
  painter'а Датацентру: стійка є одиницею колапсу, тож має малюватись де
  завгодно (діалог Колапсу, туторіал) без відтворення геометрії стіни. Має
  власний тікер — стійка, якій треба передавати годинник ззовні, це вже не
  «постав куди хочеш». `DataCentre` схуднув до кімнати: підлога, гирло шахти,
  уламки, а поверх — `Row` зі стійок; розкладку робить flex, не арифметика.
  УВАГА: висота стійки тепер задається НИЖНІМ відступом контейнера, бо
  `Expanded` віддає їй усе, що лишилось після підпису.
- **Коли painter, а коли віджет** (ревізія 2026-08-29). Painter правий там, де
  малюється те, чого віджетами не скласти: труба й біт, каміння і тріщини,
  дрейфуючі поля, `PartGlyph`/`_TrackGlyph` (вони ГЕНЕРУЮТЬ форму зі стану —
  «біт із mark+2 флейтами»), сім слотів стійки з покадровою анімацією.
  Painter був неправий там, де відтворював звичайну коробку: смуга прогресу
  (полагоджено) і `ChevronPainter`, який просто іконка (лишається).
  Відтоді відповідь на «коробка з нестандартним контуром» має ім'я: `HudBox`.
  Окремо: виноски маніпулятора — єдине місце з `TextPainter`, і бейдж із
  цифрою там описаний вдруге (перший раз віджетом у `PartFace`). Поводок усе
  одно потребує канви, але сам бейдж варто зробити спільним.
### HUD — мова пульта (рішення 2026-08-29)
Інтерфейс переведено з «застосунку» на **приладовий пульт**. Нових кольорів і
шрифтів НЕ заведено: усе те саме, змінилась лише форма — зрізані кути замість
скруглень. Живе в `ui/hud.dart`.
- **Чому взагалі:** акти Перезапуску й Колапсу були зроблені компонентом `Stat`,
  тобто найважливіші КЕРОВАНІ елементи екрана виглядали як показники, які
  випадково вимкнули. Тому й муляли чіпи «згодом» — вони були єдиним натяком на
  дію. Форма мала сказати те, що казав чіп.
- **`HudBox` — єдина поверхня, з якої вирізані всі інші.** ЧОТИРИ НЕЗАЛЕЖНІ
  осі: `corners` (які кути зрізані), `sides` (які прямі ділянки контуру
  малюються), `bracket` + `bracketArm` (жирний удар по кутах і як далеко він
  біжить уздовж сторін) і `brackets` (ЯКІ кути отримують той удар —
  2026-08-29). Четверта відділена від першої з тієї ж причини, що й решта:
  «який кут зрізаний» і «по якому вдарено» — різні питання, і форма, що вміє
  відповідати лише разом, не може бути ні цоколем, ні відкритою згори
  дужкою. Кожна форма гри — комбінація:

  | форма | corners | sides | bracket |
  |---|---|---|---|
  | плитка | за читанням | всі | — |
  | слот меню | усі 4 | лише боки | — |
  | рамка пульта | жодного | жодної | плече 13 |
  | аркуш модалки | усі 4 | всі | плече 7 |

  Правило всередині, без якого форми ламаються: **зріз малюється, якщо
  намальована хоч одна зі сторін, які він з'єднує** — інакше слот меню показує
  дві вертикалі, що висять у повітрі. Тримання осей нарізно — це те, що не дало
  файлу відростити painter на кожну форму: `_MenuPainter`, `_PlatePainter`,
  `_PanelPainter` видалені, `_BracketPainter` — 2026-08-29 (він пережив ту
  чистку і був знайдений живим, коли `HudBrackets` знадобилась четверта вісь).
  Лишився `_ChamferPainter` під `HudButton`.
- **`HudCorners` — НАБІР прапорців, не перелік** (рішення власника 2026-08-29).
  Пресети лишились (`leading` — зріз зверху-зліва й знизу-справа, `trailing` —
  дзеркально, `centred` — усі, `none`), і `HudCorners.of(align)` дає зріз за
  напрямком читання: удар там, де око входить і виходить. Але **плитка не може
  знати, що вона стоїть у групі**: п'ять плиток, кожна зі своїм зрізом, читаються
  як п'ять окремих карток — протилежне тому, чим група є. Тому `HudStat` має
  `corners`, який перебиває вибір за вирівнюванням, і головний екран б'є лише
  ЗОВНІШНІ кути блока, лишаючи всі внутрішні шви прямими.
- **`HudButton`** — зріз ДІАГОНАЛЬНОЇ пари (кнопка має напрямок) проти
  симетричних зрізів слота меню. Приймає `child` і `padding`: зміна контуру не
  має права стати зміною розмітки, тож кожне місце виклику передає свої поля,
  а ціна з іконкою їде всередину як була. Стоїть на всіх кнопках гри.
- **`HudMenu`** — горизонталі відкриті, тож ряд читається як одна смуга з
  підсвіченим слотом, а не як лінія коробок. **Таб Консолі світиться ЗЕЛЕНИМ, а
  не золотим**: золотий значить «це екран, на якому ти», зелений — «це накриє
  те, на чому ти». Береться з наявного `section.opensAsPanel`.
- **`HudProgress` проти `Gauge` — два різні типи смуги, і вони мусять
  відрізнятись на вигляд.** `Gauge` — те, що ЛЛЄТЬСЯ (енергія, знос шару):
  гладка заливка, величина неперервна. `HudProgress` — те, що ПЕРЕТИНАЮТЬ (100
  рівнів до Mk): комірки, бо важливо скільки кроків позаду і скільки лишилось.
  Комірка, яку заповнюють зараз, бліда — та сама мова, що на слотах серверів.
  Проміжки лише МІЖ комірками: перша починається на краю доріжки, остання на
  ньому ж закінчується.
  **Підпис і число живуть НАД доріжкою, а не впоперек неї (2026-08-29).**
  Покладене на комірки число потребувало плашки, щоб лишатись читабельним і
  над заливкою, і над порожнім треком, — а плашка це діра, пробита в тих самих
  комірках, які смуга й малює; читалось як наліпка. Тепер `label` зліва,
  `reading` справа, смуга під ними на всю ширину — той самий лад, що й у
  заголовків актів. Заразом зникла потреба в трюку зі зсувом на порожній
  descent. `label` не косметика: без нього смуга — це величина без іменника,
  видно «3%», але не видно чого.
- **`HudStat`** типографію не малює власну — усередині сидить домашній `Stat`,
  тож HUD-показник не може розійтися зі звичайним у кеглі чи вазі. Поля 8, не
  11: плитка, що коштує по 11 px з боків, забирає їх саме в заголовка, а
  заголовок скоротити не можна («СИМУЛЯЦ…»).
- Ще: `HudPlate` (поверхня без натискання), `HudLamp` (зелена/жовта/червона
  мова слотів сервера), `HudModal` (аркуш із зрізами й ударами по кутах — поки
  лише на Консолі, решта п'ять сидять на `GameModal`).
- **`HudChoice<T>`** (2026-08-29) — ОДИН сегментований контрол на всі вибори
  «одне з N»: частка продажу, СПИСОК/ГРУПИ, зали Торгівлі, пачка ×1/×10/макс
  на Маніпуляторі. Група володіє контуром: зрізані лише ЗОВНІШНІ кути,
  внутрішні шви — волосяні лінії, активна комірка — заливка всередині спільної
  форми, а не власна коробка (раніше кожен такий ряд був п'ятьма окремими
  слотами `HudMenu`). Прапорці `top`/`bottom` кажуть, які кути групи зрізані:
  контрол, посаджений на підлогу екрана (зали Торгівлі), тримає верх прямим —
  зріз позначає, де форма закінчується, а біля краю вона не закінчується.
  `stretch` ділить ширину порівну. `HudMenu` лишається навігації.
- **`HudProgress.from`** — колір, З ЯКОГО біжить заливка (типово бурштин).
  Дрейф порогу — єдина ХОЛОДНА смуга в грі (фіалка → сталь): це єдине
  показання на екрані, що працює на гравця, поки він нічого не тисне, і його
  не можна плутати ні з золотом кубів, ні з червоним повної стіни.
- **Урок про центрування тексту (2026-08-29):** центрувати треба ЧОРНИЛО, а не
  коробку. У показанні з самих цифр немає жодного гліфа з нижнім виносним
  елементом, але шрифт тримає під них місце, тож коробка стоїть по центру, а
  цифри — вище. Спершу пінимо `height: 1`, потім зсуваємо на порожній descent.
- **Семантика:** ряд секцій — самі іконки, і без підписів він німий для всього,
  що не бачить (читач з екрана, драйвер тестів). `Semantics(label:, button:,
  selected:)` додано.

- **`HudTap`** (2026-08-30) — домашня натискна поверхня: курсор-рука і легка
  біла плівка (5%) під ховером на десктопі, opaque hit-target скрізь.
  Плівка ЗНАЄ КОНТУР (`corners`/`cut` — той самий `_CornerClipper`), інакше
  квадрат миється поверх зрізаних кутів. ДВА уроки того ж дня: (1) механічний
  прохід переписав на HudTap і внутрішній GestureDetector самого HudTap —
  віджет будував себе, StackOverflow на першому кадрі; тепер там коментар-
  громовідвід; (2) скрими модалок і повноекранні оверлеї НЕ носять плівку
  (скрим — взагалі сирий детектор без руки: його аффорданс — хрестик, а
  рука на пів екрана оголошує контролем ТЛО). УСІ
  прості тапи гри (18 місць) проведені через нього механічним проходом по
  патерну `GestureDetector(behavior: opaque, onTap:)` — «що значить
  натисканне» на миші не може розійтись по контролах. `HudButton` говорить
  ту саму мову власноруч (ховер підсилює його ж чорнила: заливка 0.12→0.2,
  межа 0.7→1.0 — форма НЕ міняється, інакше ряди кнопок мерехтіли б).
  Порода на вибої — теж курсор-рука: це найбільша кнопка гри.
- **Пастка, що спливала тричі за сесію:** `Flexible` поруч зі `Spacer`/`Expanded`
  в одному `Row` ділять вільне місце НАВПІЛ, і права колонка не доходить до краю
  (підсумки бафів, лінія заголовка полиці, хрестик модалки). Ліки: пакувати
  ліву пару в один `Expanded`, або не робити її гнучкою взагалі.

### Збереження (рішення від 2026-08-26)
- Формат: `SaveCodec` (версія схеми + ланцюжок міграцій по сирій мапі), секції `meta` і `run`.
  `meta` (час, глибина, руда) читається окремо, щоб меню слотів описувало сейв, не
  розбираючи ран, який воно не збирається запускати.
- `PrototypeSimulation.toJson/readJson`: похідні значення НЕ пишуться. `layerHpMax`
  перераховується з формули щільності — інакше сейв міг би з нею не збігатись, а формула
  має вигравати. Форсаж не пишеться: це затиснута кнопка, її не можна зберегти.
  Відсутні ключі падають на стартові значення (сейв старішого білда завантажується).
- **Стан RNG зберігається** (`RandomSource.toJson`), тому `random` у симуляції НЕ final:
  роли продовжуються з місця, де гравець зупинився, а не переграються від сіда щозапуску.
- Слоти: `auto` + три ручних, файли в `getApplicationSupportDirectory()` (пакет
  `path_provider`). Запис атомарний — у `.writing`, потім `rename`: обрив посеред запису
  коштує НОВОГО сейву, а не старого.
- Сейв, який білд не може прочитати, ПЕРЕЙМЕНОВУЄТЬСЯ в `.unreadable`, а не стирається:
  це все ще прогрес гравця, і пізніший білд може його прочитати. Затерти його свіжим раном —
  єдиний невідновний результат.
- **Ротація бекапу**: перед записом чинний сейв копіюється в `.bak`; якщо живий файл не
  парситься — читаємо `.bak`, і лише потім здаємось. Разом з атомарним записом це закриває
  домінантну причину биття сейвів (обірваний запис), тому контрольної суми поки НЕ додаємо —
  JSON-парсер ловить обрізаний файл, а тонше пошкодження на локальному диску без мережі
  малоймовірне. Якщо колись з'явиться клауд-сейв — тоді й контрольна сума.
- **Два записи не накладаються**: `saveTo` шикується в чергу за тим, що вже пишеться
  (`_writing`), а не відкидається — ручний запис не має пропадати через те, що саме тоді
  спрацював автозбереження. Гонка двох письменників по одному файлу б'є сейв навіть при
  атомарному rename.
- Автозбереження — звичайний `Timer.periodic` (**5 хв**) + `AppLifecycleListener`
  (`onInactive`, `onHide`, `onPause`, `onDetach`, `onExitRequested`), а НЕ `TickEngine`:
  це господарство по настінному часу, пропущений запис треба забути, а не надолужити.
  Таймер — це нижня межа; реально прогрес рятує запис у мить, коли гравець відводить погляд,
  тож вікно втрати = час від останньої втрати фокуса.
- Час гри рахується монотонним `Stopwatch` і банкується в секцію `session`: переживає
  перезапуск і не зсувається системним годинником.
- **Кожна дія в меню збереження озброюється галкою.** Записати / завантажити / стерти —
  усі три незворотні (знищують або сейв, або поточний ран), тож кнопки мертві, доки гравець
  не поставить підтвердження; після дії галка сама скидається, щоб картка не лишалась
  зарядженою. Це та сама порада, що й «не роби деструктивну дію дефолтною» і «розділяй
  запис і завантаження, щоб промах мишею не коштував прогресу».
- `meta` містить заголовок для списку слотів (час, глибина, бури, рівень бура, перезапуски,
  час гри, увесь склад) — навмисне продубльований із рану, щоб перегляд сейвів ніколи не
  розбирав ран, який не збирається запускати. Пишеться з того самого стану в тому самому
  виклику, тож розійтись вони не можуть.
- Рушії стартують ТІЛЬКИ після читання сейву (`Game.start()`), інакше перший цикл
  відпрацював би по дефолтному стану, який за мить перезапишуть.
- Офлайн-нарахування при завантаженні НЕ робиться (окреме майбутнє рішення); час запису
  вже лежить у `meta`, коли дійде черга.

### Колапс (кінець циклу) — ще не в прототипах
- Late-game поріг — у СИРИХ ДАНИХ одного рана (перенасичення,
  див. «Дані, Перезапуск, Колапс»; дрейф порогу −3%/добу). Валюта: очко колапсу, +1 за Колапс (стара формула
  Ядер `(ΣКапсул·глибина·сателіти/norm)^0.33` скасована 2026-08-27).
- Скидає все крім очок колапсу, мета-дерева і Аватара. Мета-дерево: множники старту,
  кап ФО, офлайн-ефективність 25%→50%, нові механіки кожні 2–3 Колапси.

### Офлайн / Фонові обчислення (ФО)
- **Офлайн = 25% ВІД ОНЛАЙН-ТЕМПУ (переписано 2026-08-27).** Одна формула від Δt:
  `здобуто_i = yieldPerSecond(i) × секунди × 0.25`, тобто те саме джерело, що годує
  рядок «X / с» у Складі й лут-таблицю. Обидві смуги входять: відсутність платить
  чверть від того, що дав би той самий проміжок гри з утриманим пальцем. Раніше
  рахувалась ТІЛЬКИ смуга бура (`regolithPerCycle × 0.35 × цикли × 0.25`), і офлайн
  виходив ~0.6% від онлайну замість обіцяних 25% — тепер обіцянка виконується.
  НАСЛІДОК: офлайн подорожчав приблизно в 40 разів, рівно на множник перекосу
  рука/бур; коли перекіс полагодять, офлайн упаде сам, бо він похідний.
  `OfflineGain` тепер плаский `Map<ResourceId, BigDouble>` — нова руда з'являється
  у вікні сама, без нового поля. Ефективність 25% (`offlineEfficiency`). Шансові дропи —
  МАТСПОДІВАННЯМ, без ролів: тисячі тягнень зсунули б підпотоки і почервонили б тести
  паритету. Глибина офлайн НЕ росте: буріння — онлайн-гра, склад заробляє на поточному
  вибої. Пришпилено тестами.
- Розрахунок при: старті зі збереження (Δt від `meta.savedAt`) і знятті з паузи (пауза —
  теж відсутність: рушії стояли, тож span заробляє офлайн-темпом). НЕ при ручному
  завантаженні слота — інакше перезавантаження старих сейвів стало б монетним двором.
- Вікно офлайн-доходу показується від 1 хв відсутності (`offlineNoticeThreshold`);
  коротше — нарахування мовчки. Дохід зараховано ДО показу вікна: воно звітує, а не
  тримає виплату заручницею.
- Кнопки паузи/фонового режиму/збережень живуть у Консолі (сервісний люк), верхня
  стрічка — лише ресурси + шеврон складу.
- Офлайн-виробництво 20–50% темпу (старт 25%). Склад НЕСКІНЧЕННИЙ, капів на ресурси немає.
- ФО генеруються ТІЛЬКИ офлайн: 5/30хв лінійно, кап 100, overflow 10–15% темпу після капу.
- Кап ФО прокачується в мета-дереві Колапсу; темп — у дереві симуляції (гравець сам обирає
  свій ритм). Правило балансу: час до капу ≥ найдовший природний офлайн (~9–10 год).
- Витрати ФО: тимчасові бусти + дрібні перманенти. ЖОДНОЇ стаміни на базові дії.

### Аватар
- Рівень НЕ скидається нічим — ні перезапуском, ні Колапсом (2026-08-27; раніше
  скидався Колапсом). Нейроочки; рескіл БЕЗКОШТОВНИЙ і
  ТІЛЬКИ на межі — перезапуск / Колапс / рестарт циклу (білд = ставка на ран). Гілки: крит-шанс, крит-сила, ехо, фон.

### Сателіти і крафт
- Планета А — єдине місце активного буріння. Сателіти — без сцени: пасивне постачання
  ексклюзивного ресурсу + лабораторія, що відкриває якісно нове (Кріо: кріо-сканування →
  нові жили у ВЖЕ пройдених стратах А). Відкриття — one-shot гейти в дереві, один сателіт
  за раз в онбордингу.
- Крафт — стік проти інфляції: руда+ресурс сателіта → сплав → компонент. Компресія: вихід
  ×2/рівень, вартість ×3/рівень (L^1.585 soft cap). Черги працюють офлайн.

## Файли
- `docs/stratum-gdd-v1.md` — концепт-документ (розділ 10 = стартові числа балансу; УВАГА:
  розділи 2 і 7 частково застаріли — там ще шар=страта і лінійне дерево; актуальні правила —
  цей файл).
- `docs/research-report.md` — дослідження жанру: формули з реверс-інжинірингу CIFI/ISEPS/CHAD,
  бенчмарки D1/D7/D30, антипатерни. Джерело істини для балансування.
- `docs/stratum-final-design-prompt.md` — промпт, за яким робився дизайн (історія).
- `prototypes/stratum-prototype.html` — перший прототип (застарілий по метрах/дереву;
  еталон відчуття тіка).
- `prototypes/Stratum.dc.html` + `support.js` — АКТУАЛЬНА виконувана специфікація:
  1м-шари, товсті шари ×5, граф-дерево, весь тіковий рушій. Відкривається локально (обидва
  файли в одній теці). Референс поведінки №1: Flutter-ядро має відтворювати його числа.

## Структура репозиторію (рішення від 2026-08-25)
Монорепа на **pub workspace** (Dart 3.13 / Flutter 3.47): один `pubspec.lock` і один
`.dart_tool/` у корені, `pub get` виконується з кореня для всіх пакетів одразу.

```
pubspec.yaml            корінь workspace (коду не містить)
analysis_options.yaml   спільний строгий режим аналізу (strict-casts/inference/raw-types)
packages/stratum_core/  чистий Dart: тік-рушій, стан, баланс, офлайн. Без Flutter.
packages/stratum_app/   Flutter UI, платформи ТІЛЬКИ android + ios
```

- `stratum_app` залежить від `stratum_core` через `path: ../stratum_core`; зворотної
  залежності не буде ніколи.
- **Платформи (уточнено 2026-08-26): Android + Windows.** Windows — таргет релізу,
  а не лише зручність розробки. Доля iOS — відкрите питання; тека `ios/` поки є.
  Web не планується.
- Вікно на Windows: **вертикальне, пропорція телефона 9:16 замкнена**, стартова
  висота — 80% робочої області монітора, вікно центроване. Тобто на десктопі гра
  виглядає як телефон, а не як широка сцена. Реалізовано в `windows/runner/`:
  `PreferredClientSize` / `CentredOrigin` рахують стартовий розмір, `WM_SIZING`
  тримає пропорцію під час перетягування, `WM_GETMINMAXINFO` робить розгортання
  найбільшим 16:9, що влазить (інакше єдиний найімовірніший жест ламав би
  пропорцію). `Win32Window::Create` пропатчено так, щоб `size` означав
  **клієнтську область**, а не вікно з рамкою.
- **Розмітка UI — центрована телефонна колонка** (макс. ширина ~430) на тлі.
  Одна розмітка на Android і Windows; широкої desktop-розмітки не робимо.
- Програмний `SetWindowPos` пропорцію не коригує — `WM_SIZING` шле лише
  користувацьке перетягування. Для гри цього досить.
- **Чому не `window_manager`** (перевірено по його коду 2026-08-26, не з пам'яті):
  його `setAspectRatio` рахує пропорцію по прямокутнику **вікна з рамкою**, а не
  клієнтської області, тож полотно Flutter виходить нижчим на висоту заголовка;
  `WM_GETMINMAXINFO` він не обробляє взагалі, тож розгортання пропорцію ламає;
  його ж документація вимагає правити `win32_window.cpp` і `flutter_window.cpp`
  проти спалаху вікна на старті; розмір екрана вимагає другого пакета
  `screen_retriever`. Аргумент «зламає Android» — ХИБНИЙ: плагін без андроїд-
  платформи просто не потрапляє в реєстрант, впасти може лише виклик у рантаймі.
  Пакет лишається доречним, якщо колись знадобиться безрамкове вікно чи власний
  заголовок — він не конфліктує з нашим `WM_SIZING`.
- Заборона на Flutter у ядрі тримається структурно: у `stratum_core/pubspec.yaml` немає
  залежності від flutter, тому `import 'package:flutter/...'` там просто не компілюється.
- Bundle/application ID: **`ua.znz.stratum`** (Android `namespace`+`applicationId`,
  iOS `PRODUCT_BUNDLE_IDENTIFIER`). Назва застосунку — `Stratum`.
- Web/desktop таргети не генеруються. Якщо колись знадобиться web — це окреме рішення.
- Команди: `flutter pub get` (з кореня), `dart analyze` (з кореня, покриває обидва пакети),
  `dart test` (з `packages/stratum_core`), `flutter build apk --debug` (з `packages/stratum_app`).

## Примітиви ядра (рішення від 2026-08-25)
Три підсистеми в `stratum_core`, усі без рантайм-залежностей. Дизайн BigDouble і
тік-рушія — `docs/superpowers/specs/2026-08-25-bigdouble-tickengine-design.md`.

- **`BigDouble`** — мантиса+експонента, межа `±9e15`, суфікси `k…d` + scientific,
  пріоритет форматування «аргумент → стиль числа → глобальний». Ігрові гейти
  («вистачає ресурсів», «КВ ≥ поріг») ЗОБОВ'ЯЗАНІ користуватись версіями
  порівняння з допуском, інакше гравець із рівно потрібною сумою отримає відмову.
- **`TickScheduler` + `TickEngine`** — чистий крокувальник і драйвер над `Timer`.
  Час монотонний (`Stopwatch`), настінний `DateTime` належить офлайн-розрахунку
  разом із захистом від перекручування годинника. Кап наздоганяння в тіках;
  усе, що за капом, повертається викликачеві як `overflow` для офлайн-формули.
- **Граф значень (`Signal` / `Computed`)** — лінивий pull-граф з автотрекінгом
  залежностей, як Angular signals. Інвалідація через версії: каскад обривається
  на першому вузлі, чиє значення не змінилось. Вузли рана треба `dispose()` на
  перезапуску, інакше Сигнали, що переживають перезапуск, триматимуть їх вічно.
  **ПРАВИЛО (власник, 2026-08-30): УСІ похідні розрахунки ядра живуть у
  графі.** Стан, від якого хтось залежить, — `Signal`; усе вирахуване —
  `Computed`, збудований у конструкторі. Голий гетер, що читає сигнали, —
  порушення: він не кешує (перераховує кожне читання — `fundScaleOf` стоїть
  на гарячому шляху удару по п'ять смуг за раз) і не дає споживачам чесної
  інвалідації «змінилось джерело → перерахувались УСІ залежні, і ніхто
  зайвий». Прецедент: фінансування v2 спершу зібране на гетерах і переведене
  на ланцюг `_financeRound → _tranchesSpent → _financeRank → _fundCap /
  _fundGlobalScale → _fundScales[смуга]`; `tranchesGranted` при цьому мусив
  стати Signal — він залежність вільного пулу. Голим гетером може бути лише
  ЧИСТА функція констант/аргументів (`rankThreshold`, `tranchesInto`) або
  дешевá арифметика поверх уже кешованих `.value` без нових залежностей.
- **`RandomStream` / `RandomSource`** — xorshift128+ з іменованими підпотоками.
  Кожен споживач ролів («crit», «echo», «quantonium») бере власний потік: інакше
  додавання нової механіки зсуває всі роли після неї й червонить тести паритету.
  `chance(p)` тягне число ЗАВЖДИ, навіть при p=0 — саме це тримає послідовність
  стабільною при зміні чисел балансу.
- **`SaveDocument` / `SaveMigration` / `SaveCodec`** — версія схеми + ланцюжок
  міграцій по сирій мапі. Розрив у ланцюжку — помилка, а не мовчазний пропуск;
  невідомі секції проходять наскрізь, а не стираються старішим білдом.
- **`BalanceHarness`** — прогін N тіків із датчиками, вивід у CSV і в
  самодостатній HTML зі SVG-графіками (`tool/demo_report.dart` — приклад).

## Тіки: кілька незалежних циклів (рішення від 2026-08-25)
Замість одного тіка на всю гру — **окремий `TickEngine` на кожен ритм**.

- **Заряд форсажу має власний цикл**, а не росте на тіку бура (як у прототипі,
  `charge + 0.8` за тік). Цикл **зупиняється при досягненні капу** й відновлюється,
  коли гравець витратить частину. Завмерла шкала — навмисний сигнал «ти на стелі».
- Зупиняти рушій можна прямо з його ж колбека; рекурсію закриває запобіжник
  усередині `syncNow`. Без нього це був нескінченний Stack Overflow.
- `stop()` банкує накопичений час, тож пауза заморожує шкалу, а не скидає її.
- **Кап ресурсу — не привід зупиняти ГОЛОВНИЙ тік.** Він робить забагато різного
  (шкода, руда, крафт, XP), і зупинка через один переповнений ресурс заморозила б
  усе інше. Окремий цикл — можна, головний — ні.

## Годинникові домени і пауза (рішення від 2026-08-26)
**ПРАВИЛО ЧАСОЗАЛЕЖНИХ МЕХАНІК (зведено власником 2026-08-29): будь-який
функціонал, що їде на часі, зобов'язаний працювати ОДНАКОВО онлайн і офлайн.**
Реалізація одна й та сама завжди: стан — це мітка настінного часу в сейві, а
значення рахується формулою від Δt у момент читання/синку, НЕ кроками таймера.
Так уже живуть дрейф колапс-порогу (`collapseThreshold(nowMs)`), запити
Торгівлі (`syncRequests(nowMs)`: за відсутність прострочене згоряє, дошка
достеляється до слотів, без відтворення черги) і офлайн-дохід (одна формула
від Δt). Наслідок, який треба приймати свідомо: такі механіки НЕ заморожує й
пауза — таймер запиту тане під паузою так само, як дрейф. Якщо нова механіка
цього правила дотриматись не може — це привід перепроєктувати механіку, а не
виняток із правила.

**КАП ВІДСУТНОСТІ — 48 годин (рішення власника 2026-08-29).** Тиждень
відсутності гра зараховує як два дні — і в доході, і в дрейфі, і в усьому
настінному, що з'явиться далі. Реалізація — ВИЗНАНИЙ ГОДИННИК у ядрі
(`absenceCapMs`, `wallSeenMs`/`lastWallMs`, `seenNow(nowMs)`/`observeWall`):
лічильник настінного часу, що росте реальними проміжками, але кожен проміжок
затиснутий капом. `observeWall` кличуть батч бура і `_settleAbsence`; між
викликами `seenNow` екстраполює, тож показання неперервні, а пауза до 48 год
тане як і мусить. Дрейф міряється НА ЦЬОМУ годиннику (`cycleStartMs` тепер
мітка у ВИЗНАНИХ мс, не епоха; -1 = не проштамповано), офлайн-виплата — тим
самим капом у `_settleAbsence` (тому вікно чесно каже «вас не було 48 год»,
а не тиждень). Кап — ОДНА константа ядра, щоб механіки не розійшлись.
Сейв: секція `clock` {seen, last}; старий ключ `cycleStartMs` (епоха)
ігнорується — дрейф таких сейвів починається заново, бо визнаний годинник не
може чесно прочитати епохальну мітку. Пришпилено `collapse_drift_test.dart`
(кап на проміжок, кап на КОЖЕН проміжок окремо, годинник назад = нуль,
раундтрип сейву).

**ЗБІЙ СИМУЛЯЦІЇ — захист від перекрутки годинника назад (2026-08-29).**
Схема шахрайства: прокрутив добу вперед (гра чесно платить, але капом),
повернув назад — і знову «відсутність». Захист: `lastWallMs` у сейві — це
остання ЗАФІКСОВАНА мить; якщо реальний час позаду неї більш ніж на
`clockRewindTolerance` (2 хв, NTP рухає секундами — гравець годинами), гра
входить у БРІЧ: рушії стоять, поверх усього НЕЗАКРИВНА модалка «ЗБІЙ
СИМУЛЯЦІЇ» з відліком «до завершення ремонту», офлайн за час брічу НЕ
платиться (вихід банкує нульовий проміжок). Два виходи: дочекатись, поки
реальність наздожене мітку, або кнопкою «ЗБЕРЕЖЕННЯ» завантажити сейв, де
час не крутили (чистий слот знімає бріч, зіпсутий — підіймає знову).
Перевірка: кожен батч бура, старт, кожен load. КРИТИЧНА деталь, пришпилена
тестом: `observeWall` МОНОТОННИЙ — перекручений назад годинник не сміє
переписати `lastWallMs`, бо ця мітка і є доказ перекрутки; інакше перший же
observe прощав би збій. Прокрутка ВПЕРЕД шахрайством не вважається — її вже
стримує кап 48 год. Дотиснуто 2026-08-29: (1) мить запису сейву І Є міткою
останньої активності — `saveTo` банкує `observeWall` перед серіалізацією,
тож `clock.last` і `meta.savedAt` виходять з одного моменту; (2) `savedAt`
пишеться в UTC — ISO-рядок у локальному часі зсувається з таймзоною, а це ще
один годинник, який гравець може крутити (epoch-мс у `clock` таймзони не
мають); (3) під брічем НЕ пише НІХТО — ні автосейв, ні lifecycle-хуки, ні
ручний слот: прогресу під брічем немає, а слот, записаний зараз, поніс би
майбутню мітку в себе і заразив би єдине місце, куди гравець може втекти.

Три домени часу, перші два НЕ зливати:
1. **Симуляційний** — `Timer` + монотонний `Stopwatch` у `TickEngine`. Безкадровий:
   тікає при вікні без фокуса і згорнутому вікні. Якби рушії їздили на кадровому
   тікері, «офлайн» починався б від простого перемикання вікна.
2. **Презентаційний** — Flutter `Ticker`и (узори, стрічки, шестерні). Кадровий,
   глушиться `TickerMode`.
3. **Життєвий цикл** — координує перші два ОДНИМ прапором `Game.paused`: рушії
   зупиняються через `stop()`/`start()` (час банкується, шкали замерзають на місці),
   візуал — `TickerMode(enabled: !paused)` поверх усієї сцени.
- Методи, що самі будять цикли (`_syncChargeLoop`, `startForcing`), зобов'язані питати
  прапор паузи — це не латки, а їхня власна відповідальність.
- Дельта-анімації затискають покадрову дельту (`clampFrameDelta`, 66 мс): заглушений
  тікер продовжує рахувати час, і перший кадр після паузи інакше приносить усю паузу.
- **Урок замерзлої шкали (2026-08-28):** `_startTimer` більше НЕ переходить на
  `Timer.periodic`. Періодичний таймер тримає фіксовану фазу, тож будь-яка
  позачергова синхронізація між спрацюваннями (пауза, зміна темпу, зупинка на
  капі) банкує залишок інтервалу, який періодичний таймер потім проходить повз:
  він віддає рівно один інтервал за спрацювання, накопичувач замерзає на тому
  залишку, і все, що читає `progress`, показує шкалу, залиплу біля повної.
  Спостережено наживо: `pending` завмер на 1681 мс при інтервалі 1789.
  Тепер КОЖНЕ спрацювання цілиться в `timeToNextTick` і переозброюється після
  приземлення, тож залишок відпрацьовується наступним же спрацюванням.
  Пришпилено `test/energy_cadence_test.dart`.
- **Урок витоку таймера**: ланцюжок «одноразовий → періодичний» у `_startTimer`
  зобов'язаний перевіряти `identical(_timer, oneShot)` — зміна темпу зсередини тіка
  переозброює таймер, і сліпе чіпляння періодичного лишало фантомний цикл без ручки
  скасування (ресурси капали під паузою). Пришпилено регресійним тестом.
- Коли з'явиться ТРЕТІЙ рушій (крафт-черги, сателіти) — завести реєстр рушіїв у `Game`,
  щоб pause/resume/dispose ішли по списку, а не по пам'яті. На двох — не треба.

## Ризики-запобіжники (порушення = баг)
1. Жодних жорстких стін: перезапуск безпороговий (забанкувати можна завжди), а єдину стіну — колапс-поріг — з'їдає дрейф (−3%/добу).
2. Кожен перезапуск відчутний (прев'ю ≥ помітного приросту) — урок провалу CHAD.
3. Одна нова система за раз в онбордингу.
4. RNG (крит/ехо) — тільки бонус зверху, ніколи не гейт прогресу.
5. Прев'ю вигоди на кожній кнопці дії.
6. **ПОХІДНІ ВИТРАТНИКИ САМОЛІКУЮТЬСЯ НА ЧИТАННІ СЕЙВУ** (правило власника
   2026-08-30, узагальнене з фінансування). Витратник, що РАХУЄТЬСЯ, а не
   зберігається (вільні транші = раунди×платня − зважено витрачене; вільні
   патчі = цикли − витрачені; будь-який майбутній), при завантаженні
   перевіряється: якщо баланс іде в мінус або рівень вище стелі — розкладка
   неможлива за чинними числами (баланс мінявся), і ВИТРАЧАННЯ СКИДАЄТЬСЯ до
   подарованої підлоги (подарунки дерева тримаються окремим обліком і
   переживають скидання), а зароблене повертається гравцеві розлити заново.
   Гравець НІКОЛИ не успадковує борг, якого не брав. Реалізація-еталон:
   `_fundingBooksBalance`/`_resetFunding` + `fundingWasReset`, який Game
   озвучує нотисом; пришпилено тестами (зіпсутий сейв плавиться до підлоги,
   здоровий — не чіпається). Перевірено НАЖИВО: сейв власника з розкладкою
   дотарифної ери розплавився коректно при першому ж завантаженні.

## Наступні кроки (план порту, погоджено з власником)
1. Витягти з `prototypes/Stratum.dc.html` усі формули й константи в `docs/balance-spec.md`
   та машинний конфіг балансу.
2. Підняти Flutter-проєкт: пакети `stratum_core` (чистий Dart) + `stratum_app` (UI);
   big-number тип у ядрі з першого коміту.
3. Портувати тік-рушій у `stratum_core` З ТЕСТАМИ ПАРИТЕТУ: фіксований seed RNG, прогін
   N тіків, звірка чисел (руда, глибина, КВ, поріг) з еталонними значеннями, знятими з
   прототипу. «Поведінка збігається з прототипом» = зелений CI, а не побажання.
4. Офлайн-розрахунок і збереження стану (локально; формат — з думкою про майбутній
   клауд-сейв).
5. UI екрана Бура (сцена страт, тік-пульс, крити/ехо, форсаж) → далі за таймлайном ГДД §9.
6. Оновити розділи 2/7 ГДД під метри/граф.

## Як вести цей файл
Нові узгоджені рішення дописуй сюди ж (через `#`-нотатку в сесії або правкою файлу) —
CLAUDE.md є єдиним журналом рішень проєкту і їде разом із репозиторієм.